---
phase: 01-foundation-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - conftest.py
  - pyproject.toml
  - requirements-lock.txt
  - requirements.txt
  - requirements-api.txt
  - requirements-dev.txt
autonomous: true

must_haves:
  truths:
    - "pytest runs on Windows without asyncio ProactorEventLoop crash"
    - "pytest collects all tests from tests/ and src/ without crash — failures due to hardcoded paths documented for Plan 03"
    - "pip install -r requirements-lock.txt reproduces exact dependency tree"
  artifacts:
    - path: "conftest.py"
      provides: "Windows asyncio fix + session fixture"
      contains: "WindowsSelectorEventLoopPolicy"
    - path: "requirements-lock.txt"
      provides: "Pinned dependency versions"
      contains: "tensorflow==2"
    - path: "pyproject.toml"
      provides: "Updated pytest config"
      contains: "asyncio_mode"
  key_links:
    - from: "conftest.py"
      to: "pytest collection"
      via: "module-level event loop policy"
      pattern: "WindowsSelectorEventLoopPolicy"
    - from: "pyproject.toml"
      to: "pytest"
      via: "testpaths includes both tests/ and src/"
      pattern: 'testpaths.*tests.*src'
---

<objective>
Fix Python test infrastructure so pytest runs reliably on Windows and dependencies are pinned.

Purpose: Unblock all Python testing — the Windows asyncio crash prevents ANY test validation. Pinned deps ensure reproducibility.
Output: Working pytest suite, requirements-lock.txt with exact versions.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/01-foundation-stability/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin dependencies and fix pytest configuration</name>
  <files>requirements-lock.txt, requirements.txt, requirements-api.txt, requirements-dev.txt, pyproject.toml, conftest.py</files>
  <action>
  1. Create 
equirements-lock.txt at project root with exact pinned versions:
     - tensorflow==2.18.0
     - numpy==1.26.4
     - scipy==1.14.1
     - networkx==3.4.2
     - gymnasium==0.29.1
     - matplotlib==3.9.3
     - fastapi==0.115.6
     - uvicorn==0.34.0
     - pydantic==2.10.4
     - pydantic-settings==2.7.1
     - slowapi==0.1.9
     - python-multipart==0.0.18
     - python-json-logger==2.0.7
     - pytest==8.3.4
     - pytest-asyncio==0.25.2
     - pytest-cov==6.0.0
     - httpx==0.28.1
     - anyio==4.8.0
     Include ALL transitive deps by running pip freeze after install — but the core pins above MUST be present.

  2. Update 
equirements-dev.txt to add pytest-asyncio>=0.25.0 and httpx>=0.28.0.

  3. Update 
equirements-api.txt: change 	ensorflow==2.14.0 to 	ensorflow==2.18.0 and 
umpy==1.24.3 to 
umpy==1.26.4 to resolve the conflict with requirements.txt.

  4. Update pyproject.toml [tool.pytest.ini_options]:
     - Change 	estpaths = ["src"]  	estpaths = ["tests", "src"]
     - Add syncio_mode = "auto"

  5. Create conftest.py at project root with:
     - Module-level syncio.set_event_loop_policy(asyncio.WindowsSelectorEventLoopPolicy()) (runs at import time, before test collection)
     - Session-scoped autouse fixture _windows_event_loop_policy as belt-and-suspenders
     - Both guarded by sys.platform == "win32"

  Do NOT touch any src/*.py files in this task — only configuration and requirements.
  </action>
  <verify>
  - conftest.py exists at project root with WindowsSelectorEventLoopPolicy
  - 
equirements-lock.txt exists with pinned TF, numpy versions
  - pyproject.toml has 	estpaths = ["tests", "src"] and syncio_mode = "auto"
  - No syntax errors in any modified file
  </verify>
  <done>All configuration files created/updated. Dependencies pinned. Asyncio fix in place.</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies and verify Python tests pass</name>
  <files>conftest.py, pyproject.toml</files>
  <action>
  1. Install pinned dependencies: pip install -r requirements-lock.txt (if lock file has all deps) OR install from the three requirements files.

  2. Run pytest tests/ src/ -v --tb=short and capture output.

  3. If tests fail:
     - For import errors: check that testpaths and sys.path are correct
     - For asyncio crash: verify conftest.py is being loaded (add a print statement temporarily)
     - For test_api.py failures: these may need httpx TestClient — ensure httpx is installed
     - For src/test_environment.py rom environment import ...: this uses bare imports, may need CWD=src or sys.path hack in that test file specifically — if so, add a small conftest.py inside src/ that adds src/ to sys.path

  4. If all tests pass, record the count. If some fail due to issues OUTSIDE this plan's scope (e.g., hardcoded paths in test data), document them but don't fix — that's Plan 03's job.

  NOTE: Some tests in src/test_*.py use bare imports like rom road_graph import ... which only work when CWD is src/ or src/ is on sys.path. The root conftest.py should handle this via sys.path manipulation, OR create a small src/conftest.py that does sys.path.insert(0, os.path.dirname(__file__)).
  </action>
  <verify>
  - pytest runs without Windows fatal exception crash
  - Test collection succeeds (all test files found)
  - Output shows test results (pass/fail counts)
  - If any failures remain, they are documented with root cause
  </verify>
  <done>pytest runs on Windows without crash. All collectible tests either pass or have documented reasons for failure that are addressed in Plan 03.</done>
</task>

</tasks>

<verification>
1. pytest --co lists all test files from both 	ests/ and src/
2. pytest -v runs without asyncio crash on Windows
3. 
equirements-lock.txt contains exact version pins
4. pyproject.toml has updated testpaths and asyncio_mode
</verification>

<success_criteria>
- Zero Windows fatal exceptions during pytest
- Test collection finds tests in both 	ests/ and src/ directories
- 
equirements-lock.txt exists with reproducible dependency pins
- pytest-asyncio installed and configured
</success_criteria>

<output>
After completion, create .gsd/phases/01-foundation-stability/01-01-SUMMARY.md
</output>
