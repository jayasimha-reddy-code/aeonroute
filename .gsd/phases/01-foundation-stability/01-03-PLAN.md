---
phase: 01-foundation-stability
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/config.py
  - src/__init__.py
  - src/main.py
  - src/road_graph.py
  - src/traffic_generator.py
  - src/gnn_route_generator.py
  - src/q_learning_agent.py
  - src/environment.py
  - src/route_generator.py
  - src/evaluate.py
  - backend_api.py
  - src/test_traffic_generation.py
  - src/test_route_generator.py
  - src/test_road_graph.py
  - src/test_environment.py
  - src/conftest.py
autonomous: true

must_haves:
  truths:
    - "Only one canonical data/, models/, results/ directory exists at project root"
    - "All paths come from Pydantic Settings — no hardcoded path strings remain"
    - "All Python tests still pass after refactor"
    - "Backend API starts and serves requests correctly"
  artifacts:
    - path: "src/config.py"
      provides: "EVRoutingSettings class with all project paths"
      contains: "class EVRoutingSettings"
      exports: ["EVRoutingSettings", "get_settings"]
    - path: "src/__init__.py"
      provides: "Package marker making src a proper Python package"
  key_links:
    - from: "src/config.py"
      to: "src/main.py"
      via: "get_settings() import"
      pattern: "from.*config.*import.*get_settings"
    - from: "src/config.py"
      to: "backend_api.py"
      via: "get_settings() for paths and API config"
      pattern: "from src.config import.*get_settings"
    - from: "src/main.py"
      to: "src/config.py"
      via: "settings.models_path replaces hardcoded 'models'"
      pattern: "settings\\.(models|data|results)_path"
---

<objective>
Consolidate duplicate directories and replace all hardcoded paths with a Pydantic Settings configuration class.

Purpose: Eliminate confusion from duplicate data/models/results dirs. Centralize all configuration so no module contains magic path strings. This is the foundation for all subsequent phases.
Output: Single canonical directory structure, src/config.py with EVRoutingSettings, all modules updated.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/01-foundation-stability/01-RESEARCH.md
@.gsd/phases/01-foundation-stability/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate directories and create Pydantic Settings</name>
  <files>src/config.py, src/__init__.py</files>
  <action>
  1. **Merge unique files from src/ duplicates into root-level canonical dirs:**
     - Copy src/data/historical_routes/routes.npz  data/historical_routes/routes.npz (only exists in src/)
     - Copy src/models/gnn_gan/gnn_discriminator.weights.h5  models/gnn_gan/ (only exists in src/)
     - Copy src/models/gnn_gan/gnn_generator.weights.h5  models/gnn_gan/ (only exists in src/)
     - Copy src/results/plots/demo_routes.png  
esults/plots/ (if only exists in src/)
     - For files that exist in BOTH locations, keep the root-level version (it's newer/more complete for sg_gan)

  2. **Delete duplicate directories:**
     - Remove src/data/ entirely
     - Remove src/models/ entirely
     - Remove src/results/ entirely
     - Verify root-level data/, models/, 
esults/ have all needed files

  3. **Create src/__init__.py** (empty file — makes src a proper Python package)

  4. **Create src/config.py** with EVRoutingSettings:
     - _project_root() function that finds the dir containing pyproject.toml
     - EVRoutingSettings(BaseSettings) with:
       - project_root: Path (auto-detected)
       - model_dir: str = "models"
       - data_dir: str = "data"
       - 
esults_dir: str = "results"
       - rontend_dist_dir: str = "frontend/dist"
       - ML hyperparams: grid_size=10, max_battery=100.0, attery_capacity_kwh=60.0, gan_epochs=100, gan_batch_size=32, gnn_epochs=50, gnn_batch_size=16, 
l_episodes=500, 
l_max_steps=200, 	raffic_samples=500, historical_routes=300, seed=42, use_gnn_gan=True
       - API config: cors_origins, log_format, max_body_size, pi_host, pi_port
       - Properties for resolved paths: models_path, data_path, 
esults_path, rontend_dist_path, sg_gan_model_path, gnn_gan_model_path, q_learning_model_path, plots_path, metrics_path
     - model_config = {"env_prefix": "EV_", "env_file": ".env", "extra": "ignore"}
     - @lru_cache() def get_settings() -> EVRoutingSettings singleton
     - Import: rom pydantic_settings import BaseSettings (installed in Plan 01)

  5. Verify: python -c "from src.config import get_settings; s = get_settings(); print(s.models_path)" returns the correct absolute path.
  </action>
  <verify>
  - src/data/, src/models/, src/results/ directories no longer exist
  - data/historical_routes/routes.npz exists at root level
  - models/gnn_gan/ contains weight files at root level
  - src/config.py exists with EVRoutingSettings class
  - src/__init__.py exists
  - python -c "from src.config import get_settings; print(get_settings().models_path)" works
  </verify>
  <done>Duplicate directories merged and deleted. Pydantic Settings class created and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Update all modules to use Settings and verify</name>
  <files>src/main.py, src/road_graph.py, src/traffic_generator.py, src/gnn_route_generator.py, src/q_learning_agent.py, src/environment.py, src/route_generator.py, src/evaluate.py, backend_api.py, src/conftest.py</files>
  <action>
  1. **Update src/main.py:**
     - Remove sys.path.insert(0, os.path.dirname(os.path.abspath(__file__))) (line 30)
     - Change bare imports (rom road_graph import ...) to package imports (rom src.road_graph import ...)
     - Replace _default_config() method's hardcoded paths with get_settings():
       'model_dir': str(settings.models_path), etc.
     - OR better: accept settings parameter in EVRoutingSystem.__init__

  2. **Update src/evaluate.py:**
     - Remove sys.path.insert(0, ...) (line 22)
     - Change bare imports to rom src.road_graph import ... etc.
     - Use get_settings() for any path references

  3. **Update src/route_generator.py:**
     - Change try/except import blocks to direct imports: rom src.road_graph import ...
     - Remove fallback rom road_graph import ... pattern
     - Update __main__ block paths to use get_settings()

  4. **Update src/environment.py:**
     - Change rom road_graph import ... to rom src.road_graph import ...

  5. **Update src/traffic_generator.py:**
     - Update __main__ block: replace "../models/sg_gan" and "../results/plots" with get_settings().sg_gan_model_path etc.

  6. **Update src/q_learning_agent.py:**
     - Update __main__ block: replace "../models/q_learning" with get_settings().q_learning_model_path
     - Change rom environment import ... to rom src.environment import ...

  7. **Update src/road_graph.py:**
     - Update __main__ block paths to use get_settings()

  8. **Update src/gnn_route_generator.py:**
     - Change rom road_graph import ... to rom src.road_graph import ...
     - Update any hardcoded model paths

  9. **Update ackend_api.py:**
     - Remove sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'src')) (line 66)
     - Import: rom src.config import get_settings
     - Replace rontend_path = Path(__file__).parent / "frontend" / "dist" with settings.frontend_dist_path
     - Replace os.getenv("CORS_ORIGINS", ...) with settings.cors_origins
     - Replace os.getenv("LOG_FORMAT", ...) with settings.log_format
     - Replace os.getenv("MAX_BODY_SIZE", ...) with settings.max_body_size

  10. **Update test imports in src/test_*.py:**
      - src/test_traffic_generation.py: change rom traffic_generator import ...  rom src.traffic_generator import ...
      - src/test_route_generator.py: remove sys.path.insert, use rom src.route_generator import ...
      - src/test_road_graph.py: remove sys.path.insert, use rom src.road_graph import ...
      - src/test_environment.py: change rom environment import ...  rom src.environment import ...

  11. **Create src/conftest.py** (if not already exists from Plan 01) to help pytest find src package — OR verify that the root conftest.py + pyproject testpaths handles this correctly.

  12. **Run full test suite:** pytest tests/ src/ -v --tb=short
      - All Python tests must pass
      - If import errors occur, fix them

  13. **Verify backend starts:** python -c "import backend_api" should import without errors.

  IMPORTANT: Search for ANY remaining hardcoded path patterns after all changes:
  grep -rn "sys.path.insert\|'../models'\|'../results'\|'../data'\|\"../models\"\|\"../results\"\|\"../data\"" src/ backend_api.py
  This should return zero matches.
  </action>
  <verify>
  - grep -rn "sys.path.insert" src/ backend_api.py returns zero matches (except possibly in test files if absolutely needed)
  - grep -rn "'../models'\|'../results'\|'../data'" src/ returns zero matches
  - pytest tests/ src/ -v --tb=short — all tests pass
  - python -c "import backend_api" succeeds without errors
  - python -c "from src.config import get_settings; s = get_settings(); assert s.models_path.exists()" succeeds
  </verify>
  <done>All hardcoded paths replaced with Pydantic Settings. All imports standardized. Full test suite passes. Backend imports cleanly.</done>
</task>

</tasks>

<verification>
1. ls src/data src/models src/results 2>&1 — directories don't exist
2. ls data/historical_routes/routes.npz models/gnn_gan/ results/plots/ — all files present
3. python -c "from src.config import get_settings; print(get_settings().project_root)" — correct path
4. grep -rn "sys.path.insert" src/ backend_api.py — zero matches
5. pytest tests/ src/ -v — all tests pass
</verification>

<success_criteria>
- No duplicate data/models/results directories under src/
- src/config.py provides all paths via EVRoutingSettings
- Zero hardcoded paths in any Python source file
- All Python tests pass after the refactor
- Backend API imports and starts correctly
</success_criteria>

<output>
After completion, create .gsd/phases/01-foundation-stability/01-03-SUMMARY.md
</output>
