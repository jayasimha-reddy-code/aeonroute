---
phase: 01-foundation-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/Header.tsx
  - frontend/vitest.config.ts
  - frontend/index.html
autonomous: true

must_haves:
  truths:
    - "tsc --noEmit exits with zero errors"
    - "vitest run exits with zero failures"
    - "Playwright E2E tests pass against dev server"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Vitest config with setupFiles wired"
      contains: "setupFiles"
    - path: "frontend/src/App.tsx"
      provides: "Fixed TS6133 unused variable error"
    - path: "frontend/src/components/Header.tsx"
      provides: "Fixed TS6133 unused variable error"
  key_links:
    - from: "frontend/vitest.config.ts"
      to: "frontend/src/test-setup.ts"
      via: "setupFiles config"
      pattern: "setupFiles.*test-setup"
---

<objective>
Fix all frontend compilation and test errors so TypeScript, Vitest, and Playwright all pass cleanly.

Purpose: Evaluators may open DevTools — zero TS errors and passing tests signal code quality.
Output: Clean tsc, vitest, and playwright runs.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/01-foundation-stability/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TypeScript errors and wire Vitest setup</name>
  <files>frontend/src/App.tsx, frontend/src/components/Header.tsx, frontend/vitest.config.ts, frontend/index.html</files>
  <action>
  1. Run cd frontend && npx tsc --noEmit 2>&1 to get current TS errors (tsc_errors.txt may be stale).

  2. Fix each TS6133 (unused variable) error:
     - For unused catch parameters: prefix with underscore _error or remove type annotation
     - For unused destructured variables: remove from destructuring pattern
     - For unused callback params: prefix with underscore _e
     The known errors are in App.tsx (line ~31, unused e) and Header.tsx (line ~8, unused isDarkMode) but run tsc to find the CURRENT errors.

  3. Wire vitest setup file in rontend/vitest.config.ts:
     - Add setupFiles: ['./src/test-setup.ts'] inside the 	est config object
     - This enables jest-dom matchers for future tests

  4. Verify rontend/index.html <title> matches the Playwright E2E assertion pattern /EVRouteOpt/i in e2e/app.spec.ts. If it doesn't match, update the title OR the test assertion so they agree.

  5. Run 
px tsc --noEmit again — must exit with zero errors.
  </action>
  <verify>
  - cd frontend && npx tsc --noEmit exits with code 0
  - rontend/vitest.config.ts contains setupFiles
  - No unused variable warnings remain
  </verify>
  <done>TypeScript compiles cleanly in strict mode. Vitest setup file wired.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Vitest and Playwright tests pass</name>
  <files>frontend/src/__tests__/*.test.tsx, frontend/e2e/*.spec.ts</files>
  <action>
  1. Run cd frontend && npx vitest run --reporter=verbose and capture output.

  2. If any Vitest tests fail:
     - Check if failures are due to the setupFiles change (should not break existing tests)
     - Fix any import or mock issues
     - Re-run until all pass

  3. For Playwright E2E tests:
     - First ensure 
pm run dev works in the frontend directory
     - Run 
px playwright install --with-deps chromium to ensure browser is installed
     - Run 
px playwright test --reporter=list
     - E2E tests check basic page rendering and navigation — they should pass if the dev server works
     - If E2E tests fail because the backend isn't running, that's acceptable — document it
     - If E2E tests fail for other reasons (title mismatch, selector not found), fix the test assertions

  4. Record final pass/fail counts for both Vitest and Playwright.

  NOTE: E2E tests require the backend to be running for full functionality, but the app should render (with error toasts) even without the backend. If Playwright tests check for data that requires the backend, document those as known limitations.
  </action>
  <verify>
  - 
px vitest run shows all tests passing
  - Playwright test results captured (pass/documented-failures)
  - No regressions from Task 1 changes
  </verify>
  <done>Vitest unit tests all pass. Playwright E2E tests pass or have documented backend-dependency reasons for failure.</done>
</task>

</tasks>

<verification>
1. cd frontend && npx tsc --noEmit exits 0
2. cd frontend && npx vitest run shows all tests passing
3. Frontend builds successfully: cd frontend && npm run build
</verification>

<success_criteria>
- Zero TypeScript errors in strict mode
- All Vitest unit tests pass
- Playwright E2E tests pass or failures documented with root cause
- Vitest setup file properly wired
</success_criteria>

<output>
After completion, create .gsd/phases/01-foundation-stability/01-02-SUMMARY.md
</output>
