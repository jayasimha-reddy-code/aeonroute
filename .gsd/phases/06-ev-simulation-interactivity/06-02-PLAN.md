---
phase: 06-ev-simulation-interactivity
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - frontend/src/components/map/RouteTimeline.tsx
  - frontend/src/pages/RoutePlanner.tsx
  - frontend/src/components/map/MapView.tsx
  - frontend/src/components/map/NetworkNodesLayer.tsx
  - frontend/src/components/map/NetworkEdgesLayer.tsx
  - frontend/src/lib/geo.ts
  - frontend/src/services/api.ts
  - backend/app/services/routing_service.py
autonomous: true

must_haves:
  truths:
    - "Route timeline panel shows distance, estimated time, and energy at each segment"
    - "During simulation, timeline highlights the current segment"
    - "Hovering network nodes shows stats popup with degree, charging status, and coordinates"
    - "Clicking network edges reveals weight details (distance, energy cost, road type)"
  artifacts:
    - path: "frontend/src/components/map/RouteTimeline.tsx"
      provides: "Per-segment route breakdown with distance, time, energy, charging indicators"
      min_lines: 80
    - path: "frontend/src/components/map/NetworkEdgesLayer.tsx"
      provides: "Enhanced edge layer with interactiveLayerIds for click handling"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/map/RouteTimeline.tsx"
      to: "frontend/src/services/api.ts"
      via: "Route type provides path + total metrics for per-segment computation"
      pattern: "Route"
    - from: "frontend/src/components/map/MapView.tsx"
      to: "frontend/src/components/map/NetworkEdgesLayer.tsx"
      via: "Click event on edges shows popup with edge properties"
      pattern: "edges-layer"
    - from: "backend/app/services/routing_service.py"
      to: "frontend/src/lib/geo.ts"
      via: "Edge weights (distance, energy, road_type) flow from backend to GeoJSON properties"
      pattern: "distance_km|energy_kwh|road_type"
---

<objective>
Add a route timeline panel showing per-segment breakdown and enhance network interactivity with rich node/edge popups — completing the interactive exploration experience.

Purpose: The timeline makes the route data tangible (every segment visible), and the network interactivity lets evaluators explore the graph topology — both reinforce the impression of a real, data-rich system.
Output: RouteTimeline component with segment details, enhanced node hover popup, edge click popup with weights.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/06-ev-simulation-interactivity/06-01-SUMMARY.md
@frontend/src/pages/RoutePlanner.tsx
@frontend/src/components/map/MapView.tsx
@frontend/src/components/map/NetworkNodesLayer.tsx
@frontend/src/components/map/NetworkEdgesLayer.tsx
@frontend/src/lib/geo.ts
@frontend/src/services/api.ts
@backend/app/services/routing_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich edge data from backend + RouteTimeline component</name>
  <files>
    backend/app/services/routing_service.py
    frontend/src/services/api.ts
    frontend/src/lib/geo.ts
    frontend/src/components/map/RouteTimeline.tsx
  </files>
  <action>
**Backend: Enrich edge data in routing_service.py etch_road_network():**

After the dges_list is built, add edge weights from the graph:
`python
edges_list = [
    {
        "source": int(u),
        "target": int(v),
        "distance_km": round(float(d.get("distance_km", 0)), 3),
        "base_energy_kwh_per_km": round(float(d.get("base_energy_kwh_per_km", 0)), 4),
        "base_time_minutes": round(float(d.get("base_time_minutes", 0)), 2),
        "road_type": str(d.get("road_type", "local")),
    }
    for u, v, d in rg.graph.edges(data=True)
]
`
This changes the existing dges_list comprehension to include the data=True parameter and extract edge attributes.

**Frontend types: Update api.ts RoadNetworkData and add EdgeData:**
`	ypescript
export interface EdgeData {
  source: number;
  target: number;
  distance_km?: number;
  base_energy_kwh_per_km?: number;
  base_time_minutes?: number;
  road_type?: string;
}

// Update RoadNetworkData:
export interface RoadNetworkData {
  nodes: number;
  edges: number;
  charging_stations: number[];
  nodes_pos: Record<string, { x: number; y: number }>;
  edges_list: EdgeData[];  // was Array<{ source: number; target: number }>
}
`
Keep the old fields optional (distance_km?) for backward compatibility.

**Frontend geo.ts: Enrich edge GeoJSON with properties:**

Update the 
etworkToGeoJSON function's edge mapping to include edge properties:
`	ypescript
const edgeFeatures = network.edges_list
  .map((edge) => {
    const from = network.nodes_pos[edge.source.toString()];
    const to = network.nodes_pos[edge.target.toString()];
    if (!from || !to) return null;
    return {
      type: 'Feature' as const,
      properties: {
        source: edge.source,
        target: edge.target,
        distance_km: edge.distance_km ?? 0,
        energy_per_km: edge.base_energy_kwh_per_km ?? 0,
        time_minutes: edge.base_time_minutes ?? 0,
        road_type: edge.road_type ?? 'local',
      },
      geometry: {
        type: 'LineString' as const,
        coordinates: [
          toLngLat(from.x, from.y, center),
          toLngLat(to.x, to.y, center),
        ],
      },
    };
  })
  .filter((f): f is NonNullable<typeof f> => f !== null);
`

Also add a helper to compute per-segment metrics from a Route:
`	ypescript
export function computeSegmentMetrics(
  route: Route,
  posLookup: Record<string, [number, number]>,
): SegmentMetric[] {
  const segments: SegmentMetric[] = [];
  let cumulativeDistance = 0;
  let totalCoordDistance = 0;

  // First pass: compute coordinate distances for proportional distribution
  const segDistances: number[] = [];
  for (let i = 0; i < route.path.length - 1; i++) {
    const from = posLookup[route.path[i].toString()];
    const to = posLookup[route.path[i + 1].toString()];
    if (!from || !to) { segDistances.push(0); continue; }
    const d = Math.sqrt((to[0] - from[0]) ** 2 + (to[1] - from[1]) ** 2);
    segDistances.push(d);
    totalCoordDistance += d;
  }

  // Second pass: proportional metrics
  for (let i = 0; i < route.path.length - 1; i++) {
    const ratio = totalCoordDistance > 0 ? segDistances[i] / totalCoordDistance : 1 / (route.path.length - 1);
    const dist = route.distance_km * ratio;
    cumulativeDistance += dist;
    segments.push({
      fromNode: route.path[i],
      toNode: route.path[i + 1],
      distance_km: dist,
      energy_kwh: route.energy_kwh * ratio,
      time_minutes: route.time_minutes * ratio,
      cumulativeDistance_km: cumulativeDistance,
      isChargingStop: route.charging_stops.includes(route.path[i + 1]),
    });
  }
  return segments;
}

export interface SegmentMetric {
  fromNode: number;
  toNode: number;
  distance_km: number;
  energy_kwh: number;
  time_minutes: number;
  cumulativeDistance_km: number;
  isChargingStop: boolean;
}
`

**RouteTimeline component (frontend/src/components/map/RouteTimeline.tsx):**

`	ypescript
interface RouteTimelineProps {
  route: Route | null;
  posLookup: Record<string, [number, number]> | null;
  currentSegmentIndex?: number;  // from simulation state, highlights active segment
  className?: string;
}
`

Implementation:
- Import computeSegmentMetrics from ../../lib/geo
- Compute segments with useMemo
- Render a vertical timeline list inside a GlassCard. Max height ~300px with overflow-y-auto.
- Each segment row:
  - Left: vertical line connector (thin 2px line, colored by segment position: green→yellow→red)
  - Node circle: green dot for regular node, amber bolt icon for charging stop
  - Right: segment metrics — {dist.toFixed(1)} km · {energy.toFixed(2)} kWh · {time.toFixed(1)} min
  - If currentSegmentIndex matches: highlight row with g-primary-500/10 and left border accent
- Top header: "Route Timeline" with Route icon
- Bottom stats row: cumulative totals matching route data
- Show "Select a route" empty state when route is null
- Use cn() for conditional classes, ormatDistance/ormatEnergy/ormatDuration from ../../lib/utils
  </action>
  <verify>
1. Backend: cd backend && python -c "from backend.app.services.routing_service import fetch_road_network; print('OK')" — imports without error
2. Frontend: cd frontend && npx tsc --noEmit — passes
3. Route timeline renders segment breakdown when a route is selected
  </verify>
  <done>
- Backend returns edge weights (distance_km, energy, time, road_type) in road-network response
- RouteTimeline component shows per-segment breakdown with distance, time, energy
- Active segment highlights during simulation
- Charging stops marked with amber indicators
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhanced network node/edge interactivity + wire timeline</name>
  <files>
    frontend/src/components/map/MapView.tsx
    frontend/src/components/map/NetworkNodesLayer.tsx
    frontend/src/components/map/NetworkEdgesLayer.tsx
    frontend/src/pages/RoutePlanner.tsx
  </files>
  <action>
**NetworkEdgesLayer.tsx — Make edges interactive:**

1. Increase edge hit area for clickability:
   `	ypescript
   // Add a transparent wider line for easier clicking
   const edgesHitArea: LineLayerSpecification = {
     id: 'edges-hit-area',
     type: 'line',
     source: 'network-edges',
     paint: {
       'line-color': 'transparent',
       'line-width': 10,
       'line-opacity': 0,
     },
   };
   `

2. Add hover highlight style:
   `	ypescript
   const edgesHoverStyle = (isDark: boolean): LineLayerSpecification => ({
     id: 'edges-hover',
     type: 'line',
     source: 'network-edges',
     filter: ['==', ['get', 'source'], -1], // default: nothing highlighted
     paint: {
       'line-color': isDark ? '#818cf8' : '#6366f1',
       'line-width': 3,
       'line-opacity': 0.8,
     },
   });
   `

3. Add the hit area and hover layers to the rendered Source (hit area first, then edges, then hover):
   `	sx
   <Source id="network-edges" type="geojson" data={edgesGeoJSON}>
     <Layer {...edgesHitArea} />
     <Layer {...edgesLayerStyle(isDarkMode)} />
     <Layer {...edgesHoverStyle(isDarkMode)} />
   </Source>
   `

**NetworkNodesLayer.tsx — Enrich node hover data:**

Update the nodesGeoJSON to include degree information. This requires the component to accept an dgeCount map or compute from edges_list. Simpler approach: add a degree prop computed in MapView:

Add an optional 
odeDegrees property to the GeoJSON features. In geo.ts 
etworkToGeoJSON, compute node degrees:
`	ypescript
// Compute node degrees
const degrees: Record<number, number> = {};
for (const edge of network.edges_list) {
  degrees[edge.source] = (degrees[edge.source] ?? 0) + 1;
  degrees[edge.target] = (degrees[edge.target] ?? 0) + 1;
}

// Add to node features
const nodeFeatures = Object.entries(network.nodes_pos).map(([id, pos]) => ({
  ...existing,
  properties: {
    nodeId: parseInt(id),
    isCharging: network.charging_stations.includes(parseInt(id)),
    degree: degrees[parseInt(id)] ?? 0,
  },
}));
`

**MapView.tsx — Add edge click handler and enhanced popups:**

1. Add dgeHoverInfo state alongside existing hoverInfo:
   `	ypescript
   interface EdgeHoverInfo {
     lng: number;
     lat: number;
     source: number;
     target: number;
     distance_km: number;
     energy_per_km: number;
     time_minutes: number;
     road_type: string;
   }
   const [edgeHoverInfo, setEdgeHoverInfo] = useState<EdgeHoverInfo | null>(null);
   `

2. Update interactiveLayerIds on Map to include 'edges-hit-area':
   `	sx
   <Map interactiveLayerIds={['nodes-layer', 'edges-hit-area']} ... >
   `

3. Update handleClick to handle both node clicks AND edge clicks:
   `	ypescript
   const handleClick = useCallback((evt: MapLayerMouseEvent) => {
     const feature = evt.features?.[0];
     if (!feature) return;
     if (feature.layer?.id === 'nodes-layer' && feature.properties?.nodeId != null && onNodeClick) {
       onNodeClick(feature.properties.nodeId as number);
     }
     if (feature.layer?.id === 'edges-hit-area' && feature.properties?.source != null) {
       setEdgeHoverInfo({
         lng: evt.lngLat.lng,
         lat: evt.lngLat.lat,
         source: feature.properties.source,
         target: feature.properties.target,
         distance_km: feature.properties.distance_km ?? 0,
         energy_per_km: feature.properties.energy_per_km ?? 0,
         time_minutes: feature.properties.time_minutes ?? 0,
         road_type: feature.properties.road_type ?? 'unknown',
       });
     }
   }, [onNodeClick]);
   `

4. Enhance node hover popup to show more stats:
   `	sx
   {hoverInfo && (
     <Popup longitude={hoverInfo.lng} latitude={hoverInfo.lat} closeButton={false} closeOnClick={false} anchor="bottom" offset={12}>
       <div className="text-xs space-y-1 min-w-[120px]">
         <p className="font-semibold">Node {hoverInfo.nodeId}</p>
         {hoverInfo.isCharging && <p className="text-amber-500 flex items-center gap-1"><Zap className="w-3 h-3" /> Charging Station</p>}
         <p className="text-surface-500">Connections: {hoverInfo.degree}</p>
       </div>
     </Popup>
   )}
   `
   Update HoverInfo interface to include degree: number and set it from feature.properties.degree in handleMouseMove.

5. Add edge click popup (closable):
   `	sx
   {edgeHoverInfo && (
     <Popup longitude={edgeHoverInfo.lng} latitude={edgeHoverInfo.lat} closeButton={true} onClose={() => setEdgeHoverInfo(null)} anchor="bottom" offset={12}>
       <div className="text-xs space-y-1.5 min-w-[140px]">
         <p className="font-semibold">Edge {edgeHoverInfo.source} → {edgeHoverInfo.target}</p>
         <div className="space-y-0.5 text-surface-600 dark:text-surface-400">
           <p>Distance: {edgeHoverInfo.distance_km.toFixed(2)} km</p>
           <p>Energy: {edgeHoverInfo.energy_per_km.toFixed(3)} kWh/km</p>
           <p>Time: {edgeHoverInfo.time_minutes.toFixed(1)} min</p>
           <p>Type: {edgeHoverInfo.road_type}</p>
         </div>
       </div>
     </Popup>
   )}
   `

6. Import Zap from lucide-react if not already imported.

**RoutePlanner.tsx — Wire RouteTimeline:**

1. Import RouteTimeline:
   `	ypescript
   import { RouteTimeline } from '../components/map/RouteTimeline';
   `

2. Add RouteTimeline below the route cards section, before the closing </div> of the results area (lg:col-span-9):
   `	sx
   {selectedRoute && (
     <RouteTimeline
       route={selectedRoute}
       posLookup={posLookup}
       currentSegmentIndex={simState?.currentSegmentIndex}
     />
   )}
   `

This places the timeline below the route cards, visible when a route is selected. During simulation, it highlights the active segment.
  </action>
  <verify>
1. cd frontend && npx tsc --noEmit — zero errors
2. cd frontend && npx vite build — succeeds
3. Hover a node → popup shows nodeId, charging status, connection count
4. Click an edge → popup shows distance, energy/km, time, road type
5. Select a route → timeline panel appears below route cards with per-segment breakdown
6. During simulation → timeline highlights current segment
  </verify>
  <done>
- Route timeline shows per-segment distance, time, energy with charging stop indicators
- Current segment highlights during simulation
- Hovering network nodes shows stats popup (nodeId, charging status, connections)
- Clicking edges reveals weight details (distance, energy, time, road type)
- All existing functionality preserved
  </done>
</task>

</tasks>

<verification>
1. cd frontend && npx tsc --noEmit — zero TypeScript errors
2. cd frontend && npx vite build — build succeeds
3. Backend: /api/road-network response includes edge weights (distance_km, road_type, etc.)
4. Route timeline displays segment breakdown when route selected
5. Node hover popup shows degree + charging status
6. Edge click popup shows distance, energy, time, road type
7. Timeline highlights current segment during simulation
</verification>

<success_criteria>
1. Route timeline shows per-segment distance, time, energy — totals match route summary
2. Node hover popup displays nodeId, charging station status, and number of connections
3. Edge click popup displays distance_km, energy_per_km, time_minutes, and road_type
4. During EV simulation, timeline highlights the active segment in real-time
5. All Phase 04 map functionality (route drawing, energy gradient, legend) remains intact
</success_criteria>

<output>
After completion, create .gsd/phases/06-ev-simulation-interactivity/06-02-SUMMARY.md
</output>
