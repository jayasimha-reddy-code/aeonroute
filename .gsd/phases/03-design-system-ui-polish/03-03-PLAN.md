---
phase: 03-design-system-ui-polish
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - frontend/src/index.css
  - frontend/src/store/store.ts
  - frontend/src/hooks/usePresentationMode.ts
  - frontend/src/App.tsx
  - frontend/src/components/Header.tsx
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/components/StatCard.tsx
  - frontend/index.html
autonomous: true

must_haves:
  truths:
    - "Pressing Ctrl+Shift+P toggles presentation mode on and off"
    - "Presentation mode adds .presentation class to document.documentElement"
    - "Presentation mode increases contrast, thickens borders, enlarges stat values"
    - "Responsive xl breakpoint adjustments make stat values and spacing larger on projector screens"
    - "Presentation mode state persists across page refreshes via zustand persist"
    - "A visible badge in the Header indicates when presentation mode is active"
  artifacts:
    - path: "frontend/src/hooks/usePresentationMode.ts"
      provides: "Hook with Ctrl+Shift+P keyboard listener that toggles presentationMode in store"
      contains: "Ctrl"
    - path: "frontend/src/index.css"
      provides: "Presentation mode CSS overrides (.presentation class) + xl responsive tweaks"
      contains: ".presentation"
    - path: "frontend/src/store/store.ts"
      provides: "presentationMode boolean + togglePresentationMode action in zustand store"
      contains: "presentationMode"
    - path: "frontend/src/components/Header.tsx"
      provides: "Presentation mode indicator badge visible when active"
      contains: "presentation"
  key_links:
    - from: "frontend/src/hooks/usePresentationMode.ts"
      to: "frontend/src/store/store.ts"
      via: "Hook calls togglePresentationMode from store on Ctrl+Shift+P"
      pattern: "togglePresentationMode"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/hooks/usePresentationMode.ts"
      via: "App.tsx calls usePresentationMode() to register keyboard listener"
      pattern: "usePresentationMode"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/store/store.ts"
      via: "App.tsx reads presentationMode to toggle .presentation class on documentElement"
      pattern: "presentationMode"
    - from: "frontend/src/index.css"
      to: "frontend/src/App.tsx"
      via: ".presentation class on html element activates CSS overrides"
      pattern: ".presentation"
---

<objective>
Add responsive xl-breakpoint tweaks for projector display, create presentation mode CSS overrides, add presentationMode state to zustand store, build usePresentationMode keyboard hook, wire everything into App.tsx and Header.tsx.

Purpose: Ensures the app looks great on both laptop screens and projector presentations. Presentation mode (Ctrl+Shift+P) increases contrast, thickens borders, enlarges text, and simplifies animations for optimal projector viewing at distance.
Output: Pressing Ctrl+Shift+P toggles visible presentation mode with higher contrast and larger elements. XL breakpoint tweaks improve projector-resolution layout.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03-design-system-ui-polish/03-RESEARCH.md
@.gsd/phases/03-design-system-ui-polish/03-01-PLAN.md
@.gsd/phases/03-design-system-ui-polish/03-02-PLAN.md

@frontend/src/index.css
@frontend/src/store/store.ts
@frontend/src/App.tsx
@frontend/src/components/Header.tsx
@frontend/src/pages/Dashboard.tsx
@frontend/src/components/StatCard.tsx
@frontend/tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive xl tweaks + presentation mode CSS + zustand store extension</name>
  <files>
    frontend/src/index.css
    frontend/src/store/store.ts
    frontend/src/pages/Dashboard.tsx
    frontend/src/components/StatCard.tsx
  </files>
  <action>
1. **Update rontend/src/pages/Dashboard.tsx** — add xl-breakpoint responsive classes:
   - Stat card grid: add xl:gap-6 to grid gap classes (currently gap-4 sm:gap-5)
   - Page title: add xl:text-3xl to existing 	ext-2xl heading
   - Map container: if there is a fixed height like h-[420px], add xl:h-[500px]
   - Page padding: add xl:p-8 or xl:p-10 to the main page container if it uses p-4 sm:p-6 lg:p-8

2. **Update rontend/src/components/StatCard.tsx** — add responsive text sizing:
   - The stat value text (currently 	ext-2xl font-bold): add xl:text-3xl for larger display on projector screens
   - The stat title/label: add xl:text-base if currently 	ext-sm

3. **Add presentation mode CSS to rontend/src/index.css** — add a comprehensive .presentation override section. Place it AFTER the existing component layer, at the end of the file or in a dedicated section:

   `css
   /* ============================================================
      PRESENTATION MODE
      Activated via Ctrl+Shift+P — optimized for projector display
      ============================================================ */

   /* Light mode presentation overrides */
   .presentation {
     --glass-bg: rgba(255, 255, 255, 0.88);
     --glass-border: rgba(226, 232, 240, 0.9);
     --glass-blur: 12px;
   }

   /* Dark mode presentation overrides */
   .presentation.dark {
     --glass-bg: rgba(15, 23, 42, 0.85);
     --glass-border: rgba(148, 163, 184, 0.2);
     --glass-blur: 14px;
   }

   /* Thicker borders for visibility at distance */
   .presentation .card,
   .presentation .glass,
   .presentation .glass-surface {
     border-width: 2px;
   }

   /* Larger stat values */
   .presentation .stat-value {
     font-size: 2rem;
     line-height: 1.2;
   }

   /* Larger page headings */
   .presentation h1,
   .presentation .page-title {
     font-size: 2rem;
   }
   .presentation h2 {
     font-size: 1.5rem;
   }

   /* Increased spacing */
   .presentation .page-container {
     padding: 2rem;
   }

   /* Simplified CSS animations in presentation mode */
   .presentation *,
   .presentation *::before,
   .presentation *::after {
     animation-duration: 0.1s !important;
     transition-duration: 150ms !important;
   }

   /* But preserve Framer Motion animations (JS-controlled, unaffected by CSS override) */

   /* Hide non-essential UI in presentation mode */
   .presentation .hide-in-presentation {
     display: none !important;
   }

   /* Higher contrast text */
   .presentation {
     --text-primary: theme('colors.slate.950');
     --text-secondary: theme('colors.slate.700');
   }
   .presentation.dark {
     --text-primary: theme('colors.white');
     --text-secondary: theme('colors.slate.200');
   }
   `

   **Key:** The .presentation class will be toggled on document.documentElement (the <html> element) alongside the existing dark class.

4. **Update rontend/src/store/store.ts** — add presentation mode state:

   a. Add to the store interface (alongside existing 	hemeMode, isDarkMode, etc.):
   `	s
   presentationMode: boolean;
   togglePresentationMode: () => void;
   `

   b. Add initial state and action in the store creation:
   `	s
   presentationMode: false,
   togglePresentationMode: () =>
     set((s) => ({ presentationMode: !s.presentationMode })),
   `

   c. Add presentationMode to the zustand persist partialize configuration so it persists across refreshes. Find the existing partialize function (which likely includes 	hemeMode) and add presentationMode: state.presentationMode to it.

   d. Create a selector hook (alongside existing useActiveTab, useTheme, etc.):
   `	s
   export const usePresentationMode = () =>
     useAppStore((s) => ({ presentationMode: s.presentationMode, togglePresentationMode: s.togglePresentationMode }));
   `
  </action>
  <verify>
Run: cd frontend && npx tsc --noEmit 2>&1 | head -10 — zero TypeScript errors.
Run: grep "presentationMode\|togglePresentationMode" frontend/src/store/store.ts | wc -l — at least 4 matches (interface + state + action + partialize + selector).
Run: grep -c ".presentation" frontend/src/index.css — at least 10 matches (multiple CSS override rules).
Run: grep "xl:text-3xl\|xl:gap-6" frontend/src/pages/Dashboard.tsx — responsive classes present.
  </verify>
  <done>Responsive xl-breakpoint tweaks added to Dashboard and StatCard for projector-sized screens. Comprehensive .presentation CSS override section added to index.css (thicker borders, larger text, higher contrast, simplified animations). presentationMode boolean + togglePresentationMode action added to zustand store with persistence. usePresentationMode selector hook exported.</done>
</task>

<task type="auto">
  <name>Task 2: usePresentationMode hook + App.tsx wiring + Header badge</name>
  <files>
    frontend/src/hooks/usePresentationMode.ts
    frontend/src/App.tsx
    frontend/src/components/Header.tsx
  </files>
  <action>
1. **Create rontend/src/hooks/usePresentationMode.ts**:
   `	s
   import { useEffect } from 'react';
   import { usePresentationMode as usePresentationModeStore } from '../store/store';

   /**
    * Registers the Ctrl+Shift+P keyboard shortcut for toggling presentation mode.
    * Also syncs the .presentation class on document.documentElement.
    * Call this once at the App level.
    */
   export function usePresentationModeEffect(): void {
     const { presentationMode, togglePresentationMode } = usePresentationModeStore();

     // Sync .presentation class on <html>
     useEffect(() => {
       const el = document.documentElement;
       if (presentationMode) {
         el.classList.add('presentation');
       } else {
         el.classList.remove('presentation');
       }
     }, [presentationMode]);

     // Register Ctrl+Shift+P keyboard shortcut
     useEffect(() => {
       const handler = (e: KeyboardEvent) => {
         if (e.ctrlKey && e.shiftKey && e.key === 'P') {
           e.preventDefault();
           togglePresentationMode();
         }
       };
       window.addEventListener('keydown', handler);
       return () => window.removeEventListener('keydown', handler);
     }, [togglePresentationMode]);
   }
   `

   **Key details:**
   - Two separate useEffect hooks: one for class sync, one for keyboard listener
   - e.preventDefault() prevents browser print dialog from opening on Ctrl+Shift+P
   - Returns void — this is an effect-only hook, no return value needed
   - Named usePresentationModeEffect to distinguish from the store selector usePresentationMode

2. **Update rontend/src/App.tsx** — wire presentation mode:

   a. Import the hook:
   `	sx
   import { usePresentationModeEffect } from './hooks/usePresentationMode';
   `

   b. Call the hook inside the App component body (alongside existing useEffect calls):
   `	sx
   // Inside function App() { ... }
   usePresentationModeEffect();
   `

   This registers the Ctrl+Shift+P keyboard listener and syncs the .presentation class on <html>.

   c. Also read presentationMode from the store to add the presentation class immediately on load. The hook handles this, but also add it to the blocking script in index.html for instant load:
   
   Update the blocking theme script in rontend/index.html (added in Plan 03-01) to ALSO read and apply presentation mode:
   `html
   <script>
     try {
       const p = JSON.parse(localStorage.getItem('ev-routing-preferences') || '{}');
       const m = p?.state?.themeMode;
       if (m === 'dark' || (m !== 'light' && matchMedia('(prefers-color-scheme: dark)').matches)) {
         document.documentElement.classList.add('dark');
       }
       if (p?.state?.presentationMode) {
         document.documentElement.classList.add('presentation');
       }
     } catch (e) {}
   </script>
   `

3. **Update rontend/src/components/Header.tsx** — add presentation mode indicator:

   a. Import the store selector:
   `	sx
   import { usePresentationMode } from '../store/store';
   `

   b. Inside the Header component, read presentation mode state:
   `	sx
   const { presentationMode } = usePresentationMode();
   `

   c. Add a visual indicator badge — a small pill/badge that appears when presentation mode is active. Place it next to the theme toggle button or in the header's right section:
   `	sx
   {presentationMode && (
     <span className="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-xs font-medium bg-primary-500/10 text-primary-600 dark:text-primary-400 border border-primary-400/20 animate-fade-in">
       <svg className="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
         <path strokeLinecap="round" strokeLinejoin="round" d="M3.75 3v11.25A2.25 2.25 0 006 16.5h2.25M3.75 3h-1.5m1.5 0h16.5m0 0h1.5m-1.5 0v11.25A2.25 2.25 0 0118 16.5h-2.25m-7.5 0h7.5m-7.5 0l-1 3m8.5-3l1 3m0 0l.5 1.5m-.5-1.5h-9.5m0 0l-.5 1.5m.75-9l3-1 2.148 2.148A12.061 12.061 0 0116.5 7.605" />
       </svg>
       Presenting
     </span>
   )}
   `

   Alternatively, use a Monitor/Presentation lucide icon if lucide-react is already imported (it is — check the existing Header imports):
   `	sx
   import { Monitor } from 'lucide-react'; // or Presentation icon if available
   // ...
   {presentationMode && (
     <span className="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-medium bg-primary-500/10 text-primary-600 dark:text-primary-400 border border-primary-400/20">
       <Monitor className="w-3.5 h-3.5" />
       Presenting
     </span>
   )}
   `

   d. Place the badge so it's visible but not distracting — ideally to the LEFT of the theme toggle button in the header's right section.
  </action>
  <verify>
Run: cd frontend && npx tsc --noEmit 2>&1 | head -10 — zero TypeScript errors.
Run: cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -30 — all existing tests pass.
Run: grep "usePresentationModeEffect\|usePresentationMode" frontend/src/App.tsx — hook called in App.
Run: grep "Ctrl\|ctrlKey\|Shift\|shiftKey" frontend/src/hooks/usePresentationMode.ts — keyboard handler present.
Run: grep "Presenting\|presentationMode" frontend/src/components/Header.tsx — indicator badge present.
Run: grep "presentation" frontend/index.html — blocking script handles presentation class.
Run: cd frontend && npx vite build 2>&1 | tail -5 — build succeeds.
  </verify>
  <done>usePresentationModeEffect hook created with Ctrl+Shift+P keyboard shortcut and .presentation class sync. App.tsx calls hook to register listener globally. Blocking theme script in index.html also applies .presentation class on load. Header.tsx shows "Presenting" badge with icon when presentation mode is active. Build and tests pass.</done>
</task>

</tasks>

<verification>
- cd frontend && npx tsc --noEmit — zero TypeScript errors
- cd frontend && npx vitest run — all existing tests pass
- cd frontend && npx vite build — build succeeds
- grep "presentationMode" frontend/src/store/store.ts — state + action present
- grep ".presentation" frontend/src/index.css — CSS overrides present (10+ rules)
- grep "usePresentationModeEffect" frontend/src/App.tsx — hook wired in
- grep "Presenting" frontend/src/components/Header.tsx — badge present
- Visual: Press Ctrl+Shift+P  "Presenting" badge appears in header, borders thicken, text enlarges
- Visual: Press Ctrl+Shift+P again  badge disappears, normal styling restored
- Visual: Refresh page while in presentation mode  presentation mode persists (localStorage)
- Visual: On 1920x1080 viewport  stat values are larger (xl:text-3xl), grid has more spacing
</verification>

<success_criteria>
1. Pressing Ctrl+Shift+P toggles presentation mode on and off
2. Presentation mode adds .presentation class to document.documentElement
3. Presentation mode state persists across page refreshes (zustand persist)
4. CSS overrides increase contrast, thicken borders (2px), enlarge text, simplify animations
5. "Presenting" badge with icon visible in Header when presentation mode is active
6. Responsive xl breakpoint adjustments: larger stat values, increased grid gap, taller map
7. Blocking script in index.html applies presentation class before React hydrates
8. All existing Vitest and TypeScript checks pass — zero regressions
</success_criteria>

<output>
After completion, create .gsd/phases/03-design-system-ui-polish/03-03-SUMMARY.md
</output>
