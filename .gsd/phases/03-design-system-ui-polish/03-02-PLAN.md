---
phase: 03-design-system-ui-polish
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/src/lib/motion.ts
  - frontend/src/hooks/useAnimatedNumber.ts
  - frontend/src/components/ui/AnimatedNumber.tsx
  - frontend/src/components/ui/PageTransition.tsx
  - frontend/src/components/ui/index.ts
  - frontend/src/App.tsx
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/components/StatCard.tsx
autonomous: true

must_haves:
  truths:
    - "framer-motion is installed and listed in package.json dependencies"
    - "Navigating between pages shows spring-physics slide/fade transition with exit animation via AnimatePresence"
    - "Dashboard stat cards animate numbers rolling up from 0 on page load"
    - "Page transitions use LazyMotion + domAnimation for tree-shaking (~18KB instead of ~33KB)"
    - "Staggered entrance animations on dashboard stat card grid use Framer Motion spring physics"
  artifacts:
    - path: "frontend/src/lib/motion.ts"
      provides: "Centralized Framer Motion variants: pageVariants, pageTransition, staggerContainer, staggerItem"
      contains: "pageVariants"
    - path: "frontend/src/hooks/useAnimatedNumber.ts"
      provides: "Hook using useSpring + useMotionValue for animated number counting"
      contains: "useSpring"
    - path: "frontend/src/components/ui/AnimatedNumber.tsx"
      provides: "Component rendering animated number with prefix/suffix/decimals support"
      contains: "useAnimatedNumber"
    - path: "frontend/src/components/ui/PageTransition.tsx"
      provides: "AnimatePresence wrapper component for page transition orchestration"
      contains: "AnimatePresence"
    - path: "frontend/src/App.tsx"
      provides: "LazyMotion wrapper + AnimatePresence page transitions replacing CSS animate-page-enter"
      contains: "LazyMotion"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/lib/motion.ts"
      via: "App imports pageVariants and pageTransition for AnimatePresence child"
      pattern: "pageVariants|pageTransition"
    - from: "frontend/src/App.tsx"
      to: "framer-motion"
      via: "LazyMotion + domAnimation wraps app content for tree-shaking"
      pattern: "LazyMotion|domAnimation"
    - from: "frontend/src/components/StatCard.tsx"
      to: "frontend/src/components/ui/AnimatedNumber.tsx"
      via: "StatCard renders AnimatedNumber for numeric values instead of static text"
      pattern: "AnimatedNumber"
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "frontend/src/lib/motion.ts"
      via: "Dashboard stat grid uses staggerContainer + staggerItem variants"
      pattern: "staggerContainer|staggerItem"
    - from: "frontend/vite.config.ts"
      to: "framer-motion"
      via: "Manual chunk 'vendor-motion' isolates framer-motion bundle"
      pattern: "vendor-motion"
---

<objective>
Install Framer Motion, create centralized motion variants, build AnimatedNumber component with useSpring hook, wire LazyMotion + AnimatePresence page transitions into App.tsx, add Framer Motion stagger animations to Dashboard stat cards, and integrate AnimatedNumber into StatCard for rolling number effect.

Purpose: Transform static page switches into spring-physics animated transitions with exit animations, add staggered entrance effects to dashboard cards, and make stat card numbers animate from 0 to their target value — the three most visually impactful improvements.
Output: Pages transition with slide/fade spring animation, stat card numbers roll up from 0, dashboard cards stagger in with spring physics.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03-design-system-ui-polish/03-RESEARCH.md
@.gsd/phases/03-design-system-ui-polish/03-01-PLAN.md

@frontend/package.json
@frontend/vite.config.ts
@frontend/src/App.tsx
@frontend/src/pages/Dashboard.tsx
@frontend/src/components/StatCard.tsx
@frontend/src/components/ui/index.ts
@frontend/src/lib/utils.ts
@frontend/src/store/store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Framer Motion + motion variants + AnimatedNumber + Vite config</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/src/lib/motion.ts
    frontend/src/hooks/useAnimatedNumber.ts
    frontend/src/components/ui/AnimatedNumber.tsx
    frontend/src/components/ui/PageTransition.tsx
    frontend/src/components/ui/index.ts
  </files>
  <action>
1. **Install Framer Motion**:
   `ash
   cd frontend && npm install framer-motion@^11.15.0
   `
   Verify: 
pm ls framer-motion shows the installed version. Confirm package.json has "framer-motion" in dependencies.

2. **Update rontend/vite.config.ts** — add 'vendor-motion': ['framer-motion'] to the existing manualChunks object in uild.rollupOptions.output.manualChunks:
   `	s
   manualChunks: {
     'vendor-react': ['react', 'react-dom'],
     'vendor-motion': ['framer-motion'],  // NEW — add this line
     'vendor-map': ['leaflet', 'react-leaflet'],
     'vendor-charts': ['recharts'],
     'vendor-state': ['zustand', 'axios'],
   },
   `

3. **Create rontend/src/lib/motion.ts** — centralized Framer Motion variant definitions:
   `	s
   import type { Variants, Transition } from 'framer-motion';

   // Page transition variants — used with AnimatePresence in App.tsx
   export const pageVariants: Variants = {
     initial: { opacity: 0, y: 12, filter: 'blur(4px)' },
     animate: { opacity: 1, y: 0, filter: 'blur(0px)' },
     exit: { opacity: 0, y: -8, filter: 'blur(2px)' },
   };

   export const pageTransition: Transition = {
     type: 'spring',
     stiffness: 300,
     damping: 30,
   };

   // Exit transition — faster than enter for snappy feel
   export const pageExitTransition: Transition = {
     duration: 0.2,
     ease: 'easeOut',
   };

   // Stagger container — parent wraps children that should stagger in
   export const staggerContainer: Variants = {
     hidden: {},
     show: {
       transition: {
         staggerChildren: 0.06,
         delayChildren: 0.1,
       },
     },
   };

   // Stagger item — each child in a stagger container
   export const staggerItem: Variants = {
     hidden: { opacity: 0, y: 16, scale: 0.97 },
     show: {
       opacity: 1,
       y: 0,
       scale: 1,
       transition: {
         type: 'spring',
         stiffness: 400,
         damping: 25,
       },
     },
   };

   // Fade-in variant for simple opacity transitions
   export const fadeIn: Variants = {
     hidden: { opacity: 0 },
     show: { opacity: 1, transition: { duration: 0.3 } },
   };

   // Spring config presets
   export const springSnappy = { type: 'spring' as const, stiffness: 400, damping: 25 };
   export const springGentle = { type: 'spring' as const, stiffness: 200, damping: 20 };
   export const springBouncy = { type: 'spring' as const, stiffness: 300, damping: 15 };
   `

4. **Create rontend/src/hooks/useAnimatedNumber.ts**:
   `	s
   import { useEffect } from 'react';
   import { useSpring, useMotionValue, useTransform, type MotionValue } from 'framer-motion';

   interface UseAnimatedNumberOptions {
     /** Spring duration hint in seconds (default: 1.2) */
     duration?: number;
     /** Decimal places to display (default: 0) */
     decimals?: number;
     /** Spring bounce factor (default: 0) */
     bounce?: number;
     /** Whether to disable animation and show value immediately */
     disabled?: boolean;
   }

   /**
    * Animates a number from 0 (or previous value) to targetValue using spring physics.
    * Returns a MotionValue<string> that updates reactively.
    */
   export function useAnimatedNumber(
     targetValue: number,
     options: UseAnimatedNumberOptions = {},
   ): MotionValue<string> {
     const { duration = 1.2, decimals = 0, bounce = 0, disabled = false } = options;

     const motionValue = useMotionValue(disabled ? targetValue : 0);
     const springValue = useSpring(motionValue, {
       stiffness: 100,
       damping: 30,
       mass: 1,
       ...(bounce ? { bounce } : {}),
     });

     const display = useTransform(springValue, (v: number) =>
       new Intl.NumberFormat('en-US', {
         minimumFractionDigits: decimals,
         maximumFractionDigits: decimals,
       }).format(v),
     );

     useEffect(() => {
       motionValue.set(targetValue);
     }, [targetValue, motionValue]);

     return display;
   }
   `

5. **Create rontend/src/components/ui/AnimatedNumber.tsx**:
   `	sx
   import { memo } from 'react';
   import { motion, useReducedMotion } from 'framer-motion';
   import { useAnimatedNumber } from '../../hooks/useAnimatedNumber';
   import { cn } from '../../lib/utils';

   interface AnimatedNumberProps {
     /** The numeric value to animate to */
     value: number;
     /** Text to display before the number (e.g., "$") */
     prefix?: string;
     /** Text to display after the number (e.g., " kWh", "%") */
     suffix?: string;
     /** Number of decimal places (default: 0) */
     decimals?: number;
     /** Additional class names */
     className?: string;
   }

   const AnimatedNumber = memo<AnimatedNumberProps>(
     ({ value, prefix = '', suffix = '', decimals = 0, className }) => {
       const shouldReduce = useReducedMotion();
       const display = useAnimatedNumber(value, {
         decimals,
         disabled: shouldReduce ?? false,
       });

       return (
         <span className={cn('tabular-nums', className)}>
           {prefix}
           <motion.span>{display}</motion.span>
           {suffix}
         </span>
       );
     },
   );
   AnimatedNumber.displayName = 'AnimatedNumber';
   export default AnimatedNumber;
   `

6. **Create rontend/src/components/ui/PageTransition.tsx**:
   `	sx
   import { type ReactNode } from 'react';
   import { AnimatePresence, m } from 'framer-motion';
   import { pageVariants, pageTransition, pageExitTransition } from '../../lib/motion';

   interface PageTransitionProps {
     /** Unique key identifying the current page (e.g., activeTab) */
     pageKey: string;
     children: ReactNode;
   }

   /**
    * Wraps page content in AnimatePresence for enter/exit transitions.
    * Use inside a LazyMotion provider.
    */
   export default function PageTransition({ pageKey, children }: PageTransitionProps) {
     return (
       <AnimatePresence mode="wait">
         <m.div
           key={pageKey}
           initial="initial"
           animate="animate"
           exit="exit"
           variants={pageVariants}
           transition={pageTransition}
           style={{ willChange: 'opacity, transform, filter' }}
         >
           {children}
         </m.div>
       </AnimatePresence>
     );
   }
   `

   **Key details:**
   - Uses m.div (not motion.div) — lightweight alias that works with LazyMotion for tree-shaking
   - mode="wait" ensures exit completes before enter starts
   - willChange hint for GPU compositing during transition
   - pageExitTransition is imported but the exit transition is embedded in the variant's exit state for AnimatePresence to pick up automatically

7. **Update rontend/src/components/ui/index.ts** — append new exports (in addition to glass components from Plan 03-01):
   `	s
   export { default as AnimatedNumber } from './AnimatedNumber';
   export { default as PageTransition } from './PageTransition';
   `
  </action>
  <verify>
Run: cd frontend && npm ls framer-motion — shows framer-motion version installed.
Run: cd frontend && npx tsc --noEmit 2>&1 | head -10 — zero TypeScript errors.
Run: grep "vendor-motion" frontend/vite.config.ts — manual chunk present.
Run: grep "pageVariants\|staggerContainer\|staggerItem" frontend/src/lib/motion.ts — all variant exports present.
Run: grep "useSpring\|useMotionValue" frontend/src/hooks/useAnimatedNumber.ts — hook uses Framer spring.
Run: grep "AnimatePresence" frontend/src/components/ui/PageTransition.tsx — page transition wrapper exists.
  </verify>
  <done>framer-motion installed and chunked in Vite. motion.ts has all variant presets (page, stagger, fade, spring configs). useAnimatedNumber hook uses useSpring + useMotionValue. AnimatedNumber component renders animated numbers with prefix/suffix/decimals and respects reduced motion. PageTransition wraps AnimatePresence with m.div for lightweight page transitions. All new files exported from barrel.</done>
</task>

<task type="auto">
  <name>Task 2: Wire page transitions into App.tsx + Dashboard stagger + StatCard animated numbers</name>
  <files>
    frontend/src/App.tsx
    frontend/src/pages/Dashboard.tsx
    frontend/src/components/StatCard.tsx
  </files>
  <action>
1. **Update rontend/src/App.tsx** — integrate LazyMotion + PageTransition:

   a. Add imports at the top:
   `	sx
   import { LazyMotion, domAnimation } from 'framer-motion';
   import PageTransition from './components/ui/PageTransition';
   `

   b. Wrap the main app content area with <LazyMotion features={domAnimation} strict>. The LazyMotion should wrap everything that uses Framer Motion inside the return statement — place it around the Suspense/content area (NOT around Header or Sidebar).

   c. Replace the current CSS-animated page wrapper:
   `	sx
   {/* BEFORE: */}
   <Suspense fallback={<PageLoader />}>
     <div className="animate-page-enter" key={activeTab}>
       {renderView()}
     </div>
   </Suspense>

   {/* AFTER: */}
   <LazyMotion features={domAnimation} strict>
     <Suspense fallback={<PageLoader />}>
       <PageTransition pageKey={activeTab}>
         {renderView()}
       </PageTransition>
     </Suspense>
   </LazyMotion>
   `

   **Critical:** <Suspense> must be OUTSIDE <AnimatePresence> (which is inside <PageTransition>). Suspense boundaries don't work correctly inside AnimatePresence. The LazyMotion must wrap both Suspense and PageTransition.

   d. Remove the nimate-page-enter CSS class usage — the Framer Motion transition replaces it.

2. **Update rontend/src/pages/Dashboard.tsx** — add Framer Motion stagger to stat card grid:

   a. Add imports:
   `	sx
   import { m } from 'framer-motion';
   import { staggerContainer, staggerItem } from '../lib/motion';
   `

   b. Replace the stat cards grid wrapper:
   `	sx
   {/* The stat cards grid — find the existing grid with stat cards */}
   {/* BEFORE: likely <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-5 mb-8 animate-stagger"> */}

   {/* AFTER: */}
   <m.div
     className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-5 mb-8"
     variants={staggerContainer}
     initial="hidden"
     animate="show"
   >
     {/* Each stat card wrapped in m.div with staggerItem */}
     <m.div variants={staggerItem}>
       {/* ... existing StatCard ... */}
     </m.div>
     <m.div variants={staggerItem}>
       {/* ... existing StatCard ... */}
     </m.div>
     {/* ... repeat for each stat card */}
   </m.div>
   `

   c. Remove the nimate-stagger CSS class from the grid — replaced by Framer stagger.

   d. If the stat cards are rendered via .map(), wrap each map iteration in <m.div key={...} variants={staggerItem}>.

3. **Update rontend/src/components/StatCard.tsx** — integrate AnimatedNumber for numeric values:

   a. Add import:
   `	sx
   import AnimatedNumber from './ui/AnimatedNumber';
   `

   b. Modify the value display logic. The current alue prop is string | number. The StatCard should detect numeric values and render AnimatedNumber, falling back to static display for non-numeric strings:

   `	sx
   {/* Value display logic */}
   {(() => {
     if (typeof value === 'number') {
       return <AnimatedNumber value={value} className="text-2xl font-bold text-slate-900 dark:text-white" />;
     }
     // Try to parse "42.5 kWh" style strings
     const match = String(value).match(/^([^0-9]*?)([\d,.]+)(.*?)$/);
     if (match) {
       const [, prefix, numStr, suffix] = match;
       const num = parseFloat(numStr.replace(/,/g, ''));
       if (!isNaN(num)) {
         const decimals = numStr.includes('.') ? numStr.split('.')[1]?.length ?? 0 : 0;
         return (
           <AnimatedNumber
             value={num}
             prefix={prefix}
             suffix={suffix}
             decimals={decimals}
             className="text-2xl font-bold text-slate-900 dark:text-white"
           />
         );
       }
     }
     // Fallback: non-numeric string, display statically
     return <span className="text-2xl font-bold text-slate-900 dark:text-white">{value}</span>;
   })()}
   `

   c. Replace the existing static <p> or <span> that displays the value with the logic above.

   d. Keep all other StatCard contents (icon, title, subtitle, change indicator) unchanged.
  </action>
  <verify>
Run: cd frontend && npx tsc --noEmit 2>&1 | head -10 — zero TypeScript errors.
Run: cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -30 — existing tests pass (Header, Sidebar, StatCard, store tests).
Run: grep "LazyMotion\|PageTransition" frontend/src/App.tsx — both present.
Run: grep "animate-page-enter" frontend/src/App.tsx — should NOT be found (replaced by Framer Motion).
Run: grep "staggerContainer\|staggerItem\|m\.div" frontend/src/pages/Dashboard.tsx — Framer stagger present.
Run: grep "AnimatedNumber" frontend/src/components/StatCard.tsx — animated number integrated.
Run: cd frontend && npx vite build 2>&1 | tail -10 — build succeeds, check chunk sizes show vendor-motion chunk.
  </verify>
  <done>App.tsx uses LazyMotion + PageTransition with spring-physics page transitions (enter: slide up + fade, exit: slide up + blur fade). Dashboard stat card grid uses Framer Motion stagger with spring physics (0.06s stagger, spring stiffness 400). StatCard renders AnimatedNumber for numeric values (rolls from 0 to target) with fallback for non-numeric strings. CSS animate-page-enter and animate-stagger replaced with Framer Motion equivalents. Vite build produces separate vendor-motion chunk.</done>
</task>

</tasks>

<verification>
- cd frontend && npm ls framer-motion — version 11.x installed
- cd frontend && npx tsc --noEmit — zero TypeScript errors
- cd frontend && npx vitest run — all existing tests pass
- cd frontend && npx vite build — build succeeds, output shows vendor-motion chunk (~18-33KB)
- grep "LazyMotion" frontend/src/App.tsx — present
- grep "AnimatePresence" frontend/src/components/ui/PageTransition.tsx — present
- grep "AnimatedNumber" frontend/src/components/StatCard.tsx — present
- grep "staggerContainer" frontend/src/pages/Dashboard.tsx — present
- Visual: Navigate between pages  see slide/fade spring transition with exit animation
- Visual: Load dashboard  stat card numbers animate from 0 to target value
- Visual: Load dashboard  stat cards stagger in one-by-one with spring bounce
</verification>

<success_criteria>
1. framer-motion installed and listed in package.json dependencies
2. Navigating between pages shows spring-physics slide/fade with exit animation (not just CSS enter)
3. Dashboard stat card numbers animate from 0 to target value on page load (rolling effect)
4. Dashboard stat cards stagger entrance with spring physics (not CSS nth-child delays)
5. LazyMotion + domAnimation used for tree-shaking (vendor-motion chunk in build output)
6. Non-numeric StatCard values (e.g., "—") display statically without errors
7. prefers-reduced-motion is respected (AnimatedNumber disables animation)
8. All existing Vitest and TypeScript checks pass — zero regressions
</success_criteria>

<output>
After completion, create .gsd/phases/03-design-system-ui-polish/03-02-SUMMARY.md
</output>
