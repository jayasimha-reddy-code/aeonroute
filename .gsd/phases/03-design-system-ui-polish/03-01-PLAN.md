---
phase: 03-design-system-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tailwind.config.ts
  - frontend/src/index.css
  - frontend/index.html
  - frontend/src/components/ui/Skeleton.tsx
  - frontend/src/components/ui/GlassCard.tsx
  - frontend/src/components/ui/GlassButton.tsx
  - frontend/src/components/ui/GlassInput.tsx
  - frontend/src/components/ui/index.ts
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/components/Header.tsx
autonomous: true

must_haves:
  truths:
    - "Glass card components render with visible backdrop-filter: blur() and frosted border — inspectable in DevTools"
    - "Light/dark toggle switches theme with smooth crossfade — no flash of unstyled content on first load"
    - "All data-loading states show skeleton shimmer screens, never blank white space"
    - "Tailwind glass utility classes (glass-bg, glass-border, glass-blur) map to CSS custom properties"
  artifacts:
    - path: "frontend/src/components/ui/GlassCard.tsx"
      provides: "Glassmorphism card component with 4 variants (default, elevated, interactive, glow)"
      contains: "backdrop-filter"
    - path: "frontend/src/components/ui/GlassButton.tsx"
      provides: "Glass-styled button with blur background and hover glow"
      contains: "backdrop-filter"
    - path: "frontend/src/components/ui/GlassInput.tsx"
      provides: "Glass-styled input with frosted background and focus glow ring"
      contains: "backdrop-filter"
    - path: "frontend/src/components/ui/Skeleton.tsx"
      provides: "Extended skeletons: DashboardSkeleton, AnalyticsSkeleton, TrainingSkeleton composites"
      contains: "DashboardSkeleton"
    - path: "frontend/index.html"
      provides: "Blocking theme script that reads localStorage before React hydrates"
      contains: "ev-routing-preferences"
  key_links:
    - from: "frontend/tailwind.config.ts"
      to: "frontend/src/index.css"
      via: "Glass utility plugin maps to --glass-* CSS custom properties"
      pattern: "glass-bg|glass-border|glass-blur"
    - from: "frontend/src/components/ui/GlassCard.tsx"
      to: "frontend/tailwind.config.ts"
      via: "Uses glass-bg, glass-border, glass-blur utility classes"
      pattern: "glass-bg|glass-blur"
    - from: "frontend/index.html"
      to: "frontend/src/store/store.ts"
      via: "Blocking script reads same localStorage key 'ev-routing-preferences' that zustand persist uses"
      pattern: "ev-routing-preferences"
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "frontend/src/components/ui/GlassCard.tsx"
      via: "Dashboard stat cards and sidebar cards wrapped in GlassCard"
      pattern: "GlassCard"
---

<objective>
Extend the existing glass design tokens into Tailwind utility classes, create GlassCard/GlassButton/GlassInput React components, add a blocking theme script to eliminate FOUC, widen CSS transitions on surface elements for smooth theme switching, add a noise texture overlay, and extend Skeleton.tsx with page-level composite skeletons (DashboardSkeleton, AnalyticsSkeleton, TrainingSkeleton).

Purpose: Delivers the foundational visual layer — glass surfaces, theme FOUC fix, and skeleton screens — that all subsequent animation work (Plans 03-02, 03-03) builds upon. No new npm dependencies required.
Output: Glass components rendering with visible backdrop-filter blur, zero-FOUC theme toggle, complete skeleton coverage.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03-design-system-ui-polish/03-RESEARCH.md

@frontend/tailwind.config.ts
@frontend/src/index.css
@frontend/index.html
@frontend/src/components/ui/Skeleton.tsx
@frontend/src/components/ui/index.ts
@frontend/src/components/ui/Card.tsx
@frontend/src/components/StatCard.tsx
@frontend/src/pages/Dashboard.tsx
@frontend/src/components/Header.tsx
@frontend/src/store/store.ts
@frontend/src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Glass tokens + noise texture + blocking theme script + skeleton extensions</name>
  <files>
    frontend/tailwind.config.ts
    frontend/src/index.css
    frontend/index.html
    frontend/src/components/ui/Skeleton.tsx
  </files>
  <action>
1. **Extend rontend/tailwind.config.ts** — add a Tailwind plugin at the end of the plugins array that registers glass utility classes:
   `	s
   plugins: [
     // ... existing plugins ...
     function({ addUtilities }: any) {
       addUtilities({
         '.glass-bg': {
           background: 'var(--glass-bg)',
         },
         '.glass-border': {
           'border-color': 'var(--glass-border)',
         },
         '.glass-blur': {
           'backdrop-filter': 'blur(var(--glass-blur)) saturate(var(--glass-saturate))',
           '-webkit-backdrop-filter': 'blur(var(--glass-blur)) saturate(var(--glass-saturate))',
         },
         '.glass-surface': {
           'background': 'var(--glass-bg)',
           'border-color': 'var(--glass-border)',
           'backdrop-filter': 'blur(var(--glass-blur)) saturate(var(--glass-saturate))',
           '-webkit-backdrop-filter': 'blur(var(--glass-blur)) saturate(var(--glass-saturate))',
         },
       });
     },
   ]
   `

2. **Update rontend/src/index.css** — add the following in the @layer components section:

   a. **Noise texture overlay class** (adds subtle SVG noise to glass surfaces):
   `css
   .glass-noise {
     position: relative;
   }
   .glass-noise::before {
     content: '';
     position: absolute;
     inset: 0;
     background: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E") repeat;
     opacity: 0.04;
     pointer-events: none;
     border-radius: inherit;
     z-index: 0;
   }
   .dark .glass-noise::before {
     opacity: 0.06;
   }
   `

   b. **Glass elevation variants** — add these CSS custom properties to :root and .dark:
   `css
   :root {
     --glass-bg-elevated: rgba(255, 255, 255, 0.82);
     --glass-bg-sunken: rgba(255, 255, 255, 0.55);
   }
   .dark {
     --glass-bg-elevated: rgba(15, 23, 42, 0.78);
     --glass-bg-sunken: rgba(15, 23, 42, 0.45);
   }
   `

   c. **Widen CSS transition scope** on surface elements for smooth theme crossfade. Add alongside existing transition rules (DO NOT use * { transition }):
   `css
   .card, .glass, .glass-surface, .surface-raised, .surface-float,
   .btn-secondary, .input-field, .badge {
     transition: background-color var(--duration-slow, 300ms) var(--ease-out-expo, ease),
                 border-color var(--duration-normal, 200ms) ease,
                 color var(--duration-normal, 200ms) ease,
                 box-shadow var(--duration-normal, 200ms) ease;
   }
   `

3. **Add blocking theme script to rontend/index.html** — insert BEFORE any other <script> tags, inside <head>, right after <meta> tags:
   `html
   <script>
     try {
       const p = JSON.parse(localStorage.getItem('ev-routing-preferences') || '{}');
       const m = p?.state?.themeMode;
       if (m === 'dark' || (m !== 'light' && matchMedia('(prefers-color-scheme: dark)').matches)) {
         document.documentElement.classList.add('dark');
       }
     } catch (e) {}
   </script>
   `
   This reads the same ev-routing-preferences key that zustand persist uses (via zustand/middleware persist config with 
ame: 'ev-routing-preferences'). It sets the dark class on <html> before React hydrates, preventing FOUC.

4. **Extend rontend/src/components/ui/Skeleton.tsx** — add 3 page-level composite skeleton components at the bottom of the file (AFTER existing StatCardSkeleton and RouteCardSkeleton):

   a. **DashboardSkeleton**: Matches Dashboard.tsx layout — 4 stat card skeletons in a responsive grid + a large rectangular skeleton for the map area + 2 smaller rectangular skeletons for sidebar cards.
   
   b. **AnalyticsSkeleton**: 4 stat card skeletons in grid + 2 chart placeholder rectangles (h-64 each) + a table skeleton (6 rows of text lines).
   
   c. **TrainingSkeleton**: 3 model status card skeletons (rectangular, h-24 each) + a controls area skeleton (buttons + progress bar).

   Each composite skeleton must:
   - Use the existing Skeleton base component and StatCardSkeleton where applicable
   - Match the grid classes of their real page counterpart (e.g., grid-cols-1 sm:grid-cols-2 lg:grid-cols-4)
   - Be exported from the file as named exports
   - Include proper aria-label="Loading" for accessibility
  </action>
  <verify>
Run: cd frontend && npx tsc --noEmit 2>&1 | head -5 — zero TypeScript errors related to new/modified files.
Run: grep -c "glass-bg\|glass-border\|glass-blur\|glass-surface" frontend/tailwind.config.ts — at least 4 matches.
Run: grep "ev-routing-preferences" frontend/index.html — blocking script present.
Run: grep -c "DashboardSkeleton\|AnalyticsSkeleton\|TrainingSkeleton" frontend/src/components/ui/Skeleton.tsx — at least 3 matches (one per export).
Run: grep "glass-noise" frontend/src/index.css — noise texture class present.
  </verify>
  <done>Tailwind glass utilities registered. Noise texture CSS class added. Glass elevation variants (--glass-bg-elevated, --glass-bg-sunken) defined. Surface element transitions widened for smooth theme crossfade. Blocking theme script in index.html prevents FOUC. DashboardSkeleton, AnalyticsSkeleton, TrainingSkeleton composites added to Skeleton.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: GlassCard + GlassButton + GlassInput components + Dashboard integration</name>
  <files>
    frontend/src/components/ui/GlassCard.tsx
    frontend/src/components/ui/GlassButton.tsx
    frontend/src/components/ui/GlassInput.tsx
    frontend/src/components/ui/index.ts
    frontend/src/pages/Dashboard.tsx
    frontend/src/components/Header.tsx
  </files>
  <action>
1. **Create rontend/src/components/ui/GlassCard.tsx**:
   `	sx
   import { forwardRef, type ReactNode, type HTMLAttributes } from 'react';
   import { cn } from '../../lib/utils';

   interface GlassCardProps extends HTMLAttributes<HTMLDivElement> {
     variant?: 'default' | 'elevated' | 'interactive' | 'glow';
     padding?: 'none' | 'sm' | 'md' | 'lg';
     noise?: boolean;
     children: ReactNode;
   }

   const variantStyles: Record<string, string> = {
     default: 'glass-surface border glass-border rounded-xl shadow-card',
     elevated: 'border rounded-xl shadow-elevated bg-[var(--glass-bg-elevated)] glass-border glass-blur',
     interactive: 'glass-surface border glass-border rounded-xl shadow-card hover:shadow-elevated hover:scale-[1.02] hover:border-primary-400/30 active:scale-[0.99] cursor-pointer',
     glow: 'glass-surface border glass-border rounded-xl shadow-card hover:shadow-glow hover:border-primary-400/40',
   };

   const paddingStyles: Record<string, string> = {
     none: '',
     sm: 'p-3',
     md: 'p-4 sm:p-5',
     lg: 'p-5 sm:p-6 lg:p-8',
   };

   const GlassCard = forwardRef<HTMLDivElement, GlassCardProps>(
     ({ variant = 'default', padding = 'md', noise = false, className, children, ...props }, ref) => (
       <div
         ref={ref}
         className={cn(
           variantStyles[variant],
           paddingStyles[padding],
           'transition-all duration-200',
           noise && 'glass-noise',
           className,
         )}
         {...props}
       >
         {children}
       </div>
     ),
   );
   GlassCard.displayName = 'GlassCard';
   export default GlassCard;
   `

   **Key details:**
   - Uses the glass-surface utility class from Task 1 (combines glass-bg + glass-border + glass-blur in one)
   - Forwards refs for DOM access
   - cn() from lib/utils for class merging (existing utility)
   - 
oise prop adds the glass-noise class for SVG noise texture overlay
   - interactive variant includes hover scale + border glow (mirrors existing .card-interactive behavior)
   - glow variant includes shadow-glow (already in tailwind.config.ts shadows)
   - elevated variant uses --glass-bg-elevated (more opaque for higher elevation)
   - Accepts all standard div HTML attributes via HTMLAttributes<HTMLDivElement>

2. **Create rontend/src/components/ui/GlassButton.tsx**:
   `	sx
   import { forwardRef, type ButtonHTMLAttributes, type ReactNode } from 'react';
   import { cn } from '../../lib/utils';

   interface GlassButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
     variant?: 'default' | 'primary' | 'ghost';
     size?: 'sm' | 'md' | 'lg';
     children: ReactNode;
   }

   const variantStyles: Record<string, string> = {
     default: 'glass-surface border glass-border hover:bg-white/90 dark:hover:bg-slate-800/80',
     primary: 'glass-blur bg-primary-500/80 border border-primary-400/30 text-white hover:bg-primary-500/95 shadow-btn-primary',
     ghost: 'glass-blur bg-transparent border border-transparent hover:glass-bg hover:border-[var(--glass-border)]',
   };

   const sizeStyles: Record<string, string> = {
     sm: 'px-3 py-1.5 text-sm rounded-lg',
     md: 'px-4 py-2 text-sm rounded-xl',
     lg: 'px-6 py-2.5 text-base rounded-xl',
   };

   const GlassButton = forwardRef<HTMLButtonElement, GlassButtonProps>(
     ({ variant = 'default', size = 'md', className, children, ...props }, ref) => (
       <button
         ref={ref}
         className={cn(
           variantStyles[variant],
           sizeStyles[size],
           'font-medium transition-all duration-200 active:scale-[0.97] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-400/50 focus-visible:ring-offset-2 focus-visible:ring-offset-transparent',
           className,
         )}
         {...props}
       >
         {children}
       </button>
     ),
   );
   GlassButton.displayName = 'GlassButton';
   export default GlassButton;
   `

3. **Create rontend/src/components/ui/GlassInput.tsx**:
   `	sx
   import { forwardRef, type InputHTMLAttributes } from 'react';
   import { cn } from '../../lib/utils';

   interface GlassInputProps extends InputHTMLAttributes<HTMLInputElement> {
     error?: boolean;
   }

   const GlassInput = forwardRef<HTMLInputElement, GlassInputProps>(
     ({ error, className, ...props }, ref) => (
       <input
         ref={ref}
         className={cn(
           'w-full px-4 py-2.5 rounded-xl border text-sm',
           'glass-surface glass-border',
           'placeholder:text-slate-400 dark:placeholder:text-slate-500',
           'focus:outline-none focus:ring-2 focus:ring-primary-400/40 focus:border-primary-400/50',
           'transition-all duration-200',
           error && 'border-danger-500/60 focus:ring-danger-400/40',
           className,
         )}
         {...props}
       />
     ),
   );
   GlassInput.displayName = 'GlassInput';
   export default GlassInput;
   `

4. **Update rontend/src/components/ui/index.ts** — append 3 new exports:
   `	s
   export { default as GlassCard } from './GlassCard';
   export { default as GlassButton } from './GlassButton';
   export { default as GlassInput } from './GlassInput';
   `
   Also add named exports of the new skeleton composites:
   `	s
   export { DashboardSkeleton, AnalyticsSkeleton, TrainingSkeleton } from './Skeleton';
   `

5. **Update rontend/src/pages/Dashboard.tsx** — replace the existing stat card wrappers with GlassCard:
   - Import GlassCard from ../components/ui/GlassCard (or from the barrel ../components/ui)
   - Find the stat cards grid section (4 stat cards in grid-cols-1 sm:grid-cols-2 lg:grid-cols-4)
   - Wrap each StatCard in <GlassCard variant="interactive" padding="none"> (StatCard already has its own padding)
   - For any sidebar info cards that use <div className="card ...">, replace with <GlassCard> to apply glassmorphism
   - Replace the loading fallback with <DashboardSkeleton /> from the skeleton extensions (if not already using it)
   - Keep the existing nimate-stagger class on the grid — Plan 03-02 will replace it with Framer Motion

6. **Verify Header.tsx glass integration**:
   - The Header component already has a sticky header with background classes. Confirm it uses glass-like styling (e.g., ackdrop-blur, g-white/80, dark:bg-slate-900/80 or similar).
   - If the header does NOT already have glass styling, add glass-surface glass-border border-b classes to give it the frosted header look.
   - Do NOT break the existing theme toggle button or navigation — only add visual glass treatment.
  </action>
  <verify>
Run: cd frontend && npx tsc --noEmit 2>&1 | head -10 — zero TypeScript errors.
Run: cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -20 — existing tests still pass (no regressions).
Run: grep "backdrop-filter\|glass-surface\|glass-blur" frontend/src/components/ui/GlassCard.tsx — glass styling present in GlassCard.
Run: grep "GlassCard\|GlassButton\|GlassInput" frontend/src/components/ui/index.ts — all 3 exported from barrel.
Run: grep "GlassCard" frontend/src/pages/Dashboard.tsx — GlassCard used in Dashboard.
  </verify>
  <done>GlassCard, GlassButton, GlassInput components created with proper variants, ref forwarding, and accessibility. Barrel index.ts exports all new components + skeleton composites. Dashboard.tsx uses GlassCard for stat cards and sidebar cards. Header.tsx has glass styling on sticky header. All existing tests pass.</done>
</task>

</tasks>

<verification>
- cd frontend && npx tsc --noEmit — zero TypeScript errors across all files
- cd frontend && npx vitest run — all existing tests pass (no regressions from glass component additions)
- grep -r "backdrop-filter" frontend/src/components/ui/Glass*.tsx — all 3 glass components have backdrop-filter
- grep "ev-routing-preferences" frontend/index.html — blocking theme script present in head
- grep "DashboardSkeleton" frontend/src/components/ui/Skeleton.tsx — composite skeleton exists
- grep "GlassCard" frontend/src/pages/Dashboard.tsx — dashboard uses glass cards
- Visual: Open http://localhost:5173, inspect a stat card in DevTools  Computed Styles  backdrop-filter shows blur() value
- Visual: Open in incognito with dark system preference  page loads dark immediately, no white flash
</verification>

<success_criteria>
1. GlassCard.tsx renders with visible backdrop-filter: blur() and frosted border — inspectable in DevTools
2. GlassButton.tsx and GlassInput.tsx render with glass aesthetic (blur background, themed borders)
3. Blocking theme script in index.html prevents FOUC — dark mode loads instantly without white flash
4. Surface elements (.card, .glass, .surface-raised etc.) have CSS transitions for smooth theme crossfade
5. DashboardSkeleton, AnalyticsSkeleton, TrainingSkeleton composites match real page layouts
6. Dashboard stat cards are wrapped in GlassCard interactive variant
7. All existing Vitest and TypeScript checks pass — zero regressions
</success_criteria>

<output>
After completion, create .gsd/phases/03-design-system-ui-polish/03-01-SUMMARY.md
</output>
