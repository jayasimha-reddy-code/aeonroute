# Plan 11-02: App Shell, Sidebar & Header Navigation

## Wave 2 — Shell (After Foundation)

<objective>
Rebuild the application shell — Sidebar, Header, App.tsx layout — to match the reference image's premium cockpit navigation. The sidebar is a ~200px expanded panel with "EV Routing System" branding, user status section, icon+text nav items, and a bottom user row. The header bar has the page title on the left and action icons (view toggles, dropdowns) on the right. The main content area uses the bento grid foundation for pages.
</objective>

<context>
**Reference image — Sidebar (all 4 panels, left edge):**

The sidebar is consistent across all 4 reference panels:
1. **Width:** ~200-220px (NOT 80px icon-only). Expanded with both icons and text labels.
2. **Background:** Same glass surface as cards — `bg-white/[0.02] backdrop-blur-2xl border-r border-white/[0.05]`
3. **Top section:** 
   - ⚡ Logo icon (emerald) + "EV Routing System" text in white, bold
   - User section below: "User" label in muted text, green dot + "Status" with a dropdown chevron
4. **Navigation items (vertical list):**
   - Each item: icon + text label, side-by-side (NOT icon-above-label)
   - **Active item:** Emerald left accent bar + `bg-emerald-dim text-emerald` background tint + white text
   - **Inactive items:** `text-slate-400` muted, on hover `text-white bg-white/[0.04]`
   - Items visible in reference: Dashboard (grid icon), Map (map icon), Overview (layout icon), Stations (zap icon), Settings (gear icon)
5. **Bottom bar:**
   - User row: Green dot + "Malur" username + status text
   - Two small icon buttons (arrows/repeat + settings gear)
6. **Sidebar icons:** From the reference, the sidebar also shows additional icons further down — these appear to be section separators or secondary nav (brain icon for AI, target icon, etc.)

**Reference image — Header bar (top of each panel):**
1. **Left:** Page title in large semibold white text ("Dashboard", "Route Planner", "Analytics")
2. **Right:** Small icon group:
   - Two toggle icons (grid/list view toggle, compact icon)
   - Dropdown selector or filter
   - These appear as subtle `bg-white/[0.04] rounded-lg` icon buttons
3. **Height:** ~56-64px
4. **Background:** Transparent — sits flush with the content area, no visible bar

**Reference image — Header (Route Planner, top-right panel):**
- Right side also shows tab pills: "Fastest | Eco | Scenic" — page-specific controls in the header
- User avatar dots (green for online) at the right end

**Current state:**
- `Sidebar.tsx` (204 lines): 260px expandable sidebar with collapse toggle, `primary-600` active state, `surface-*` colors, `dark:` prefixes, AI Stack footer
- `Header.tsx` (122 lines): Theme toggle (Sun/Moon/Monitor cycler), status pill, presentation mode badge, gradient logo
- `App.tsx` (116 lines): `useTheme()`, dark mode toggling, gradient backgrounds
</context>

<dependencies>
Plan 11-01 (CSS Tokens & Grid Setup) must be complete.
</dependencies>

---

## Tasks

<task id="1">
  <name>Rewrite App.tsx — bento shell layout</name>
  <files>frontend/src/App.tsx</files>
  <action>
    Rewrite the entire component. Remove all theme logic, dark mode effects, gradient backgrounds.

    **New structure:**
    ```tsx
    import { lazy, Suspense } from 'react';
    import { LazyMotion, domAnimation } from 'framer-motion';
    import { useActiveTab } from './store/store';
    import Header from './components/Header';
    import Sidebar from './components/Sidebar';
    import { ToastContainer } from './components/ToastContainer';
    import { PageLoader } from './components/ui';

    const Dashboard = lazy(() => import('./pages/Dashboard'));
    const RoutePlanner = lazy(() => import('./pages/RoutePlanner'));
    const Training = lazy(() => import('./pages/Training'));
    const Analytics = lazy(() => import('./pages/Analytics'));

    const pages = { dashboard: Dashboard, routing: RoutePlanner, training: Training, analytics: Analytics };

    export default function App() {
      const activeTab = useActiveTab();
      const Page = pages[activeTab] || Dashboard;

      return (
        <LazyMotion features={domAnimation}>
          {/* Skip link */}
          <a href="#main" className="sr-only focus:not-sr-only focus:absolute focus:z-50 focus:p-3 focus:bg-emerald focus:text-midnight focus:rounded-lg">
            Skip to content
          </a>

          <div className="flex h-screen bg-midnight overflow-hidden">
            {/* Sidebar — fixed width */}
            <Sidebar />

            {/* Main area — flex column */}
            <div className="flex-1 flex flex-col min-w-0">
              <Header />
              <main id="main" className="flex-1 overflow-y-auto overflow-x-hidden p-4 lg:p-6">
                <Suspense fallback={<PageLoader />}>
                  <Page />
                </Suspense>
              </main>
            </div>
          </div>

          <ToastContainer />
        </LazyMotion>
      );
    }
    ```

    **Removed:**
    - `useTheme()`, `isDarkMode`, `useEffect` for dark class toggling
    - `usePresentationMode` import
    - Gradient background classes
    - `transition-colors duration-300`
    - `PageTransition` wrapper (keep Suspense for code splitting, remove AnimatePresence page transitions — they add visual noise)
    - `dark:` prefixes

    **Kept:**
    - LazyMotion for Framer Motion tree-shaking
    - Lazy page imports with Suspense + PageLoader fallback
    - Skip-to-content accessibility link
    - ToastContainer
  </action>
  <verify>
    - Under 45 lines
    - Zero `dark:` prefixes
    - Zero `useTheme` or `useEffect` for theme
    - Root div: `flex h-screen bg-midnight overflow-hidden`
    - Sidebar renders before main content in DOM order
  </verify>
</task>

<task id="2">
  <name>Rewrite Sidebar.tsx — premium expanded navigation</name>
  <files>frontend/src/components/Sidebar.tsx</files>
  <action>
    Complete rewrite to match the reference sidebar. ~200px wide, expanded, with branding + user section + nav items + bottom bar.

    **New Sidebar structure:**
    ```tsx
    import { useActiveTab, useSetActiveTab } from '../store/store';
    import { LayoutDashboard, Map, BarChart3, Zap, Settings, Brain, Target, Activity, ChevronDown } from 'lucide-react';
    import { cn } from '../lib/utils';

    const navItems = [
      { id: 'dashboard', label: 'Dashboard', icon: LayoutDashboard },
      { id: 'routing', label: 'Map', icon: Map },
      { id: 'analytics', label: 'Overview', icon: BarChart3 },
      { id: 'stations', label: 'Stations', icon: Zap },
      { id: 'settings', label: 'Settings', icon: Settings },
    ] as const;

    const secondaryNav = [
      { icon: Brain, label: 'AI Models' },
      { icon: Target, label: 'Routing' },
      { icon: Activity, label: 'Monitoring' },
    ];

    export default function Sidebar() {
      const activeTab = useActiveTab();
      const setActiveTab = useSetActiveTab();

      return (
        <aside className="w-56 h-screen flex flex-col bg-white/[0.02] backdrop-blur-2xl border-r border-white/[0.05] flex-shrink-0">

          {/* ── Brand ── */}
          <div className="p-5 pb-3">
            <div className="flex items-center gap-2.5 mb-4">
              <div className="w-8 h-8 rounded-lg bg-emerald-dim flex items-center justify-center">
                <Zap className="w-4.5 h-4.5 text-emerald" />
              </div>
              <span className="text-sm font-bold text-white tracking-tight">EV Routing System</span>
            </div>

            {/* User status dropdown */}
            <button className="w-full flex items-center gap-2 px-3 py-2 rounded-xl bg-white/[0.03] border border-white/[0.05] hover:bg-white/[0.06] transition-all duration-300">
              <span className="w-2 h-2 rounded-full bg-emerald animate-pulse-glow" />
              <div className="flex-1 text-left">
                <p className="text-xs text-label">User</p>
                <p className="text-xs font-medium text-white">Status</p>
              </div>
              <ChevronDown className="w-3.5 h-3.5 text-label" />
            </button>
          </div>

          {/* ── Primary nav ── */}
          <nav className="flex-1 px-3 py-2 space-y-1" role="navigation" aria-label="Main">
            {navItems.map(item => {
              const isActive = activeTab === item.id;
              return (
                <button
                  key={item.id}
                  onClick={() => setActiveTab(item.id)}
                  className={cn(
                    'w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all duration-300',
                    isActive
                      ? 'bg-emerald-dim text-emerald shadow-glow-emerald/20'
                      : 'text-label hover:text-white hover:bg-white/[0.04]'
                  )}
                  aria-current={isActive ? 'page' : undefined}
                >
                  <item.icon className="w-[18px] h-[18px] flex-shrink-0" />
                  <span>{item.label}</span>
                  {isActive && (
                    <span className="ml-auto w-1.5 h-1.5 rounded-full bg-emerald" />
                  )}
                </button>
              );
            })}

            {/* Separator */}
            <div className="!my-4 h-px bg-white/[0.05]" />

            {/* Secondary nav icons */}
            {secondaryNav.map(item => (
              <button
                key={item.label}
                className="w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm text-label hover:text-white hover:bg-white/[0.04] transition-all duration-300"
              >
                <item.icon className="w-[18px] h-[18px] flex-shrink-0" />
                <span>{item.label}</span>
              </button>
            ))}
          </nav>

          {/* ── Bottom bar ── */}
          <div className="p-3 border-t border-white/[0.05]">
            <div className="flex items-center gap-2.5 px-3 py-2">
              <span className="w-2 h-2 rounded-full bg-emerald" />
              <div className="flex-1 min-w-0">
                <p className="text-xs font-medium text-white truncate">User status</p>
                <p className="text-[10px] text-label truncate">Online</p>
              </div>
              <Settings className="w-4 h-4 text-label hover:text-white cursor-pointer transition-colors" />
            </div>
          </div>
        </aside>
      );
    }
    ```

    **Key design decisions from reference:**
    - Width: `w-56` (224px) — matches the ~200-220px visible in reference
    - Background: `bg-white/[0.02] backdrop-blur-2xl` — same glass as cards
    - Right border: `border-r border-white/[0.05]` — subtle edge
    - Active nav: `bg-emerald-dim text-emerald` — green tint background + green text + small green dot indicator
    - Nav items: icon + text label side-by-side (NOT icon above label)
    - User status: Dropdown button with green dot + chevron
    - Bottom bar: Username + status text + settings icon
    - Separator line between primary and secondary nav
    - Mobile: For now, sidebar is always visible. Mobile overlay can be added as a follow-up.

    **Removed from old sidebar:**
    - Collapse toggle + `sidebarCollapsed` state
    - `ChevronLeft` / `X` icons for collapse
    - Mobile overlay with `bg-black/40`
    - AI Stack footer
    - `primary-600` active state
    - All `dark:` prefixes
    - Keyboard arrow-key navigation (keep this TODO for accessibility pass)
  </action>
  <verify>
    - Sidebar is `w-56` (~224px), NOT 80px or 260px
    - Active nav uses `bg-emerald-dim text-emerald`
    - User status dropdown at top with green dot + "Status" + chevron
    - Bottom bar has username + settings icon
    - No `dark:` prefixes, no `var(--`, no `primary-*` colors
    - No collapse toggle functionality
  </verify>
</task>

<task id="3">
  <name>Rewrite Header.tsx — minimal title + action bar</name>
  <files>frontend/src/components/Header.tsx</files>
  <action>
    Complete rewrite. The reference header is minimal: page title left, small action icons right.

    **New Header structure:**
    ```tsx
    import { useActiveTab } from '../store/store';
    import { LayoutGrid, List, SlidersHorizontal } from 'lucide-react';

    const pageTitles: Record<string, string> = {
      dashboard: 'Dashboard',
      routing: 'Route Planner',
      training: 'Training',
      analytics: 'Analytics',
    };

    export default function Header() {
      const activeTab = useActiveTab();
      const title = pageTitles[activeTab] || 'Dashboard';

      return (
        <header className="h-14 flex items-center justify-between px-6 border-b border-white/[0.03] flex-shrink-0">
          {/* Page title */}
          <h1 className="text-lg font-semibold text-white tracking-tight">{title}</h1>

          {/* Action icons */}
          <div className="flex items-center gap-2">
            <button className="w-8 h-8 rounded-lg bg-white/[0.04] border border-white/[0.06] flex items-center justify-center text-label hover:text-white hover:bg-white/[0.08] transition-all duration-300" title="Grid view">
              <LayoutGrid className="w-4 h-4" />
            </button>
            <button className="w-8 h-8 rounded-lg bg-white/[0.04] border border-white/[0.06] flex items-center justify-center text-label hover:text-white hover:bg-white/[0.08] transition-all duration-300" title="List view">
              <List className="w-4 h-4" />
            </button>
            <button className="w-8 h-8 rounded-lg bg-white/[0.04] border border-white/[0.06] flex items-center justify-center text-label hover:text-white hover:bg-white/[0.08] transition-all duration-300" title="Filters">
              <SlidersHorizontal className="w-4 h-4" />
            </button>
          </div>
        </header>
      );
    }
    ```

    **Removed from old header:**
    - Theme toggle (Sun/Moon/Monitor cycler) — permanently dark
    - Zap icon gradient logo badge
    - Status pill (online/offline) — moved to sidebar 
    - Presentation mode badge
    - Bell notification icon
    - `useTheme`, `usePresentationMode` hooks
    - All `dark:` prefixes
    - `.glass` bar styling (header is now transparent/borderless)

    **Design from reference:**
    - Ultra-minimal header — just the page title and small utility buttons
    - Background: transparent (no bar visible, just content + subtle bottom border)
    - Action buttons: small square icons with glass bg
    - Height: 56px (`h-14`)
  </action>
  <verify>
    - Header is under 35 lines
    - No theme toggle (no Sun/Moon icons)
    - No status pill
    - Page title uses `useActiveTab()` to determine name
    - Action buttons use `bg-white/[0.04]` glass style
    - No `dark:` prefixes
  </verify>
</task>

<task id="4">
  <name>Update store — remove theme, fix tab types</name>
  <files>frontend/src/store/store.ts</files>
  <action>
    **Remove:**
    - `ThemeMode` type (was `'light' | 'dark' | 'system'`)
    - `themeMode` from state
    - `setThemeMode` action
    - `useTheme` selector
    - `themeMode` from persist partialize

    **Add** (if not already present):
    - `'stations'` and `'settings'` to `AppTab` type (for new nav items — these will show empty states for now)
    - `useSetActiveTab` selector if not already exported

    **Keep everything else** unchanged — routes, training, toasts, EV state, SSE, etc.

    The goal is minimal deletions to avoid breaking API/training functionality. Only theme-related code is removed.
  </action>
  <verify>
    - `ThemeMode` type is gone
    - `useTheme` is not exported
    - `themeMode` not in state or persist
    - `AppTab` includes at least `'dashboard' | 'routing' | 'training' | 'analytics'`
    - `useSetActiveTab` is exported
    - All existing hooks (useRoadNetwork, useRoutes, useTrainingProgress, etc.) still export correctly
  </verify>
</task>

<task id="5">
  <name>Clean up ToastContainer.tsx</name>
  <files>frontend/src/components/ToastContainer.tsx</files>
  <action>
    Update styling to use new tokens:
    - Container: keep `fixed bottom-4 right-4 z-[100]`
    - Toast card: `bg-white/[0.04] backdrop-blur-2xl border border-white/[0.06] rounded-2xl p-4 shadow-card`
    - Close button: `text-label hover:text-white`
    - Success icon: `text-emerald`
    - Warning icon: `text-amber`
    - Error icon: `text-rose`
    - Info icon: `text-blue`
    - Remove old `var(--` inline styles
    - Remove `dark:` prefixes
    - Keep slide-in animation
  </action>
  <verify>
    - No `var(--` inline styles
    - No `dark:` prefixes
    - Uses `text-emerald`, `text-amber`, `text-rose`, `text-blue` for icon colors
    - Card uses `bg-white/[0.04] backdrop-blur-2xl rounded-2xl`
  </verify>
</task>

<task id="6" type="checkpoint:human-verify" gate="blocking">
  <what-built>Rebuilt the app shell — Sidebar (224px wide, EV Routing System branding, icon+text nav, user status section, bottom bar), Header (page title + action icons), App.tsx (clean flex layout). Theme toggling fully removed. Toast container updated.</what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev`
    2. **Sidebar:** ~224px wide, visible on left
       - "EV Routing System" with ⚡ logo at top
       - User status dropdown with green dot
       - Nav items: Dashboard, Map, Overview, Stations, Settings — with icons + text
       - Active item (Dashboard) is green-tinted with emerald text
       - Clicking nav items switches pages and updates the active indicator
       - Bottom bar shows user status + settings icon
    3. **Header:** Page title on left ("Dashboard"), 3 small icon buttons on right
       - Title changes when switching tabs
       - No theme toggle, no status pill
    4. **Layout:** Sidebar + main area fill the screen. No scrollbar on the shell itself.
    5. **Overall:** Ultra-dark midnight background, glass sidebar, minimal header. Everything feels "cockpit-like."
  </how-to-verify>
  <resume-signal>Type "approved" if the shell matches the reference, or describe adjustments needed.</resume-signal>
</task>
