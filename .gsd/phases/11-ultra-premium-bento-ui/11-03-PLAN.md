# Plan 11-03: Interactive Primitives, Animations & Glass Components

## Wave 3 — Primitives (After Shell)

<objective>
Rebuild every UI primitive component (GlassCard, Button, Input, Badge, ProgressBar, Spinner, Skeleton, EmptyState) plus create new premium primitives (Toggle Switch, Slider, Progress Ring, Stat Card) with fluid micro-interactions. Every interactive element gets `transition-all duration-500 ease-out`. Cards lift on hover with emerald glow. Buttons have press feedback. Inputs have glowing focus rings. All primitives use the flat token system from 11-01.
</objective>

<context>
**Reference image — Interaction patterns:**

1. **Cards (everywhere):** Ultra-translucent `bg-white/[0.02]`, heavy blur, on hover lift `-translate-y-1` and glow `shadow-glow-emerald`. All state transitions are 500ms ease-out — slow, fluid, premium.
2. **Stat cards (Dashboard top row):**
   - "Network Nodes": Large number (`text-3xl font-bold text-white`) + subtitle badge (`+5% this week` in emerald)
   - "Avg Energy": Number + sparkline mini-chart inline
   - "Avg Time": Number with formatted time display
   - "Traffic factor": Small circular progress ring (33%) inside the card
3. **Toggle switches (Route Planner):**
   - "Avoid Tolls" (off state): Dark track `bg-white/[0.06]`, circle thumb on left
   - "Optimize for Battery" (on state): Green track `bg-emerald`, white circle thumb on right, slight glow
   - Size: ~40px wide, ~22px tall
4. **Sliders (Training config):**
   - "Learning Rate 0.55": Emerald filled track, white/emerald thumb with glow
   - Track: `bg-white/[0.06] rounded-full h-1.5`
   - Filled portion: `bg-emerald rounded-full`
   - Thumb: `w-4 h-4 bg-emerald rounded-full shadow-glow-emerald cursor-pointer`
5. **Progress rings (Training — Temp/Load gauges):**
   - Circular SVG gauge with colored fill arc
   - Center: icon or percentage text
   - Sizes: ~80px diameter
   - Colors: Emerald (good), Amber (moderate), Rose (critical)
6. **Buttons (throughout):**
   - Primary: `bg-emerald text-midnight font-semibold rounded-xl px-5 py-2.5`
   - Ghost: `text-label hover:text-white hover:bg-white/[0.04] rounded-xl`
   - Danger: `bg-rose-dim text-rose border border-rose/20 rounded-xl`
   - All have `active:scale-[0.97]` press feedback
   - "Start training" button: Full-width emerald with subtle glow on hover
7. **Inputs (Route Planner — waypoints):**
   - Glass background: `bg-white/[0.03] border border-white/[0.06] rounded-xl`
   - Focus: `border-emerald/40 shadow-glow-emerald/10 ring-1 ring-emerald/20`
   - Has ❌ remove button on right for multi-stop items
   - Placeholder: `text-slate-500`
8. **Badges (throughout):**
   - Emerald badge: `bg-emerald-dim text-emerald text-xs font-medium rounded-full px-2.5 py-0.5`
   - Amber: `bg-amber-dim text-amber`
   - Rose: `bg-rose-dim text-rose`
   - Small dot indicator before text in some badges

**Current component inventory (to be rewritten):**
- `ui/Card.tsx` (50 lines) — 5 variants, needs simplification to glass-only
- `ui/GlassCard.tsx` (46 lines) — CSS var driven, broken after 11-01
- `ui/GlassButton.tsx` (42 lines) — `.glass-surface` references, broken
- `ui/GlassInput.tsx` (26 lines) — `.glass-surface` references, broken
- `ui/Button.tsx` (69 lines) — 5 variants, needs token swap
- `ui/Input.tsx` (67 lines) — `.input-field` CSS class, broken
- `ui/Badge.tsx` (48 lines) — 6 variants with old colors
- `ui/ProgressBar.tsx` (62 lines) — 5 color variants, old tokens
- `ui/Spinner.tsx` (28 lines) — `text-primary-500`, broken
- `ui/Skeleton.tsx` (187 lines) — `.skeleton-shimmer`, `.card` classes
- `ui/EmptyState.tsx` (43 lines) — `bg-surface-100`, broken
- `ui/AnimatedNumber.tsx` (37 lines) — fine, keep as-is
- `ui/PageTransition.tsx` (30 lines) — possibly remove or simplify
- `ui/PageLoader.tsx` (38 lines) — `.card` containers, broken
- `StatCard.tsx` (73 lines) — `.card` class, old color variants
</context>

<dependencies>
Plan 11-01 (tokens) and Plan 11-02 (shell) must be complete.
</dependencies>

---

## Tasks

<task id="1">
  <name>Rebuild Card.tsx + GlassCard.tsx → single Card primitive</name>
  <files>frontend/src/components/ui/Card.tsx, frontend/src/components/ui/GlassCard.tsx</files>
  <action>
    Merge into a single `Card.tsx`. Delete `GlassCard.tsx` (update barrel export in ui/index.ts).

    **New Card.tsx:**
    ```tsx
    import { forwardRef, type HTMLAttributes } from 'react';
    import { cn } from '../../lib/utils';

    interface CardProps extends HTMLAttributes<HTMLDivElement> {
      hover?: boolean;
      glow?: 'emerald' | 'amber' | 'rose' | 'cyan' | 'none';
      padding?: 'none' | 'sm' | 'md' | 'lg';
    }

    const glowMap = {
      emerald: 'shadow-glow-emerald border-emerald/20',
      amber: 'shadow-glow-amber-top border-t-amber/30',
      rose: 'shadow-glow-rose border-rose/20',
      cyan: 'shadow-glow-cyan border-cyan/20',
      none: '',
    };

    const paddingMap = {
      none: '',
      sm: 'p-4',
      md: 'p-5',
      lg: 'p-6',
    };

    export const Card = forwardRef<HTMLDivElement, CardProps>(({
      hover = false,
      glow = 'none',
      padding = 'md',
      className,
      children,
      ...props
    }, ref) => (
      <div
        ref={ref}
        className={cn(
          'bg-white/[0.02] backdrop-blur-2xl border border-white/[0.05] rounded-2xl shadow-card',
          hover && 'glass-hover',
          glowMap[glow],
          paddingMap[padding],
          className,
        )}
        {...props}
      >
        {children}
      </div>
    ));

    Card.displayName = 'Card';
    export default Card;
    ```

    Also update `ui/index.ts` barrel:
    - Remove `GlassCard` export
    - Add `Card` as both named and default
    - Add `export { Card as GlassCard }` for backward compatibility during migration
  </action>
  <verify>
    - Single `Card.tsx` file
    - `GlassCard.tsx` deleted or emptied
    - Base classes: `bg-white/[0.02] backdrop-blur-2xl border border-white/[0.05] rounded-2xl shadow-card`
    - `hover` prop adds `glass-hover` class (lift + glow on hover)
    - `glow='amber'` produces amber top-edge glow (for AI Model Status card)
    - No `dark:` prefixes, no `var(--`
  </verify>
</task>

<task id="2">
  <name>Rebuild Button.tsx + GlassButton.tsx → single Button</name>
  <files>frontend/src/components/ui/Button.tsx, frontend/src/components/ui/GlassButton.tsx</files>
  <action>
    Merge into a single `Button.tsx`. Delete `GlassButton.tsx`.

    **New Button.tsx:**
    ```tsx
    import { forwardRef, type ButtonHTMLAttributes } from 'react';
    import { Loader2, type LucideIcon } from 'lucide-react';
    import { cn } from '../../lib/utils';

    interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
      variant?: 'primary' | 'secondary' | 'ghost' | 'danger' | 'outline';
      size?: 'sm' | 'md' | 'lg';
      icon?: LucideIcon;
      iconRight?: LucideIcon;
      loading?: boolean;
      fullWidth?: boolean;
    }

    const variants = {
      primary: 'bg-emerald text-midnight font-semibold hover:shadow-glow-emerald active:scale-[0.97]',
      secondary: 'bg-white/[0.04] text-white border border-white/[0.06] hover:bg-white/[0.08] active:scale-[0.97]',
      ghost: 'text-label hover:text-white hover:bg-white/[0.04]',
      danger: 'bg-rose-dim text-rose border border-rose/20 hover:bg-rose/20 active:scale-[0.97]',
      outline: 'border border-white/[0.08] text-label hover:text-white hover:border-white/[0.15] hover:bg-white/[0.03]',
    };

    const sizes = {
      sm: 'text-xs px-3 py-1.5 gap-1.5',
      md: 'text-sm px-4 py-2.5 gap-2',
      lg: 'text-base px-6 py-3 gap-2.5',
    };

    export const Button = forwardRef<HTMLButtonElement, ButtonProps>(({
      variant = 'primary', size = 'md', icon: Icon, iconRight: IconRight,
      loading, fullWidth, className, children, disabled, ...props
    }, ref) => (
      <button
        ref={ref}
        disabled={disabled || loading}
        className={cn(
          'inline-flex items-center justify-center rounded-xl font-medium',
          'transition-all duration-500 ease-out',
          'disabled:opacity-40 disabled:pointer-events-none',
          variants[variant],
          sizes[size],
          fullWidth && 'w-full',
          className,
        )}
        {...props}
      >
        {loading ? <Loader2 className="w-4 h-4 animate-spin" /> : Icon && <Icon className="w-4 h-4" />}
        {children}
        {IconRight && !loading && <IconRight className="w-4 h-4" />}
      </button>
    ));

    Button.displayName = 'Button';
    export default Button;
    ```

    Key: Every button has `transition-all duration-500 ease-out` — the slow, fluid premium feel. `active:scale-[0.97]` gives press feedback. `hover:shadow-glow-emerald` on primary gives the emerald glow.
  </action>
  <verify>
    - Single `Button.tsx` file
    - `GlassButton.tsx` deleted
    - Primary variant: `bg-emerald text-midnight`
    - All variants have `transition-all duration-500 ease-out`
    - `active:scale-[0.97]` on pressable variants
    - `rounded-xl` (NOT `rounded-full` — reference shows rounded rectangles, not pills)
    - `disabled:opacity-40`
  </verify>
</task>

<task id="3">
  <name>Rebuild Input.tsx + GlassInput.tsx → single Input</name>
  <files>frontend/src/components/ui/Input.tsx, frontend/src/components/ui/GlassInput.tsx</files>
  <action>
    Merge into single `Input.tsx`. Delete `GlassInput.tsx`.

    **New Input.tsx:**
    ```tsx
    import { forwardRef, type InputHTMLAttributes } from 'react';
    import { type LucideIcon } from 'lucide-react';
    import { cn } from '../../lib/utils';

    interface InputProps extends InputHTMLAttributes<HTMLInputElement> {
      label?: string;
      error?: string;
      icon?: LucideIcon;
      iconRight?: LucideIcon;
      onIconRightClick?: () => void;
    }

    export const Input = forwardRef<HTMLInputElement, InputProps>(({
      label, error, icon: Icon, iconRight: IconRight, onIconRightClick,
      className, id, ...props
    }, ref) => (
      <div className="space-y-1.5">
        {label && (
          <label htmlFor={id} className="block text-xs font-medium text-label">
            {label}
          </label>
        )}
        <div className="relative">
          {Icon && <Icon className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-label" />}
          <input
            ref={ref}
            id={id}
            className={cn(
              'w-full bg-white/[0.03] border border-white/[0.06] rounded-xl px-3.5 py-2.5 text-sm text-white',
              'placeholder:text-muted',
              'focus:border-emerald/40 focus:ring-1 focus:ring-emerald/20 focus:shadow-glow-emerald/10',
              'transition-all duration-300',
              Icon && 'pl-10',
              IconRight && 'pr-10',
              error && 'border-rose/40 focus:border-rose/60 focus:ring-rose/20',
              className,
            )}
            {...props}
          />
          {IconRight && (
            <button
              type="button"
              onClick={onIconRightClick}
              className="absolute right-3 top-1/2 -translate-y-1/2 text-label hover:text-white transition-colors"
            >
              <IconRight className="w-4 h-4" />
            </button>
          )}
        </div>
        {error && <p className="text-xs text-rose">{error}</p>}
      </div>
    ));

    Input.displayName = 'Input';
    export default Input;
    ```

    **Key:** `focus:border-emerald/40 focus:ring-1 focus:ring-emerald/20` — emerald glow on focus matching reference.
  </action>
  <verify>
    - Single `Input.tsx` file
    - `GlassInput.tsx` deleted
    - Base: `bg-white/[0.03] border border-white/[0.06] rounded-xl`
    - Focus: `border-emerald/40 ring-1 ring-emerald/20`
    - Error: `border-rose/40`
    - Label: `text-xs font-medium text-label`
    - No `.input-field` CSS class usage
  </verify>
</task>

<task id="4">
  <name>Create ToggleSwitch.tsx primitive</name>
  <files>frontend/src/components/ui/ToggleSwitch.tsx</files>
  <action>
    New component matching the Route Planner reference toggle switches.

    ```tsx
    import { cn } from '../../lib/utils';

    interface ToggleSwitchProps {
      checked: boolean;
      onChange: (checked: boolean) => void;
      label?: string;
      disabled?: boolean;
      size?: 'sm' | 'md';
    }

    export function ToggleSwitch({ checked, onChange, label, disabled, size = 'md' }: ToggleSwitchProps) {
      const trackSize = size === 'sm' ? 'w-8 h-[18px]' : 'w-10 h-[22px]';
      const thumbSize = size === 'sm' ? 'w-3.5 h-3.5' : 'w-4 h-4';
      const thumbTranslate = size === 'sm' ? 'translate-x-[14px]' : 'translate-x-[18px]';

      return (
        <label className={cn('inline-flex items-center gap-3 cursor-pointer', disabled && 'opacity-40 pointer-events-none')}>
          <button
            role="switch"
            aria-checked={checked}
            onClick={() => onChange(!checked)}
            disabled={disabled}
            className={cn(
              'relative inline-flex items-center rounded-full transition-all duration-500 ease-out',
              trackSize,
              checked ? 'bg-emerald shadow-glow-emerald/30' : 'bg-white/[0.06]',
            )}
          >
            <span
              className={cn(
                'absolute left-[3px] rounded-full bg-white shadow-sm transition-transform duration-500 ease-out',
                thumbSize,
                checked && thumbTranslate,
              )}
            />
          </button>
          {label && <span className="text-sm text-label">{label}</span>}
        </label>
      );
    }

    export default ToggleSwitch;
    ```

    **Key:** On state has `bg-emerald shadow-glow-emerald/30` — green track with subtle glow. Off state has `bg-white/[0.06]` — barely visible dark track. Thumb slides with 500ms ease-out.
  </action>
  <verify>
    - Track on: `bg-emerald` with glow
    - Track off: `bg-white/[0.06]`
    - Thumb: white circle, slides with 500ms
    - Has `role="switch"` and `aria-checked` for accessibility
  </verify>
</task>

<task id="5">
  <name>Create Slider.tsx primitive</name>
  <files>frontend/src/components/ui/Slider.tsx</files>
  <action>
    Custom slider matching Training configuration reference (Learning Rate, Batch Size sliders).

    ```tsx
    import { useRef, useCallback, type MouseEvent } from 'react';
    import { cn } from '../../lib/utils';

    interface SliderProps {
      value: number;
      min?: number;
      max?: number;
      step?: number;
      onChange: (value: number) => void;
      label?: string;
      displayValue?: string;
      color?: 'emerald' | 'amber' | 'cyan';
    }

    export function Slider({ value, min = 0, max = 100, step = 1, onChange, label, displayValue, color = 'emerald' }: SliderProps) {
      const trackRef = useRef<HTMLDivElement>(null);
      const percent = ((value - min) / (max - min)) * 100;

      const colorMap = {
        emerald: { fill: 'bg-emerald', thumb: 'bg-emerald shadow-glow-emerald', text: 'text-emerald' },
        amber: { fill: 'bg-amber', thumb: 'bg-amber shadow-glow-amber', text: 'text-amber' },
        cyan: { fill: 'bg-cyan', thumb: 'bg-cyan shadow-glow-cyan', text: 'text-cyan' },
      };

      return (
        <div className="space-y-2">
          {(label || displayValue) && (
            <div className="flex items-center justify-between">
              {label && <span className="text-xs font-medium text-label">{label}</span>}
              {displayValue && <span className={cn('text-xs font-mono font-medium', colorMap[color].text)}>{displayValue}</span>}
            </div>
          )}
          <div className="relative" ref={trackRef}>
            <input
              type="range"
              min={min}
              max={max}
              step={step}
              value={value}
              onChange={(e) => onChange(Number(e.target.value))}
              className="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10"
            />
            {/* Track */}
            <div className="h-1.5 bg-white/[0.06] rounded-full overflow-hidden">
              <div
                className={cn('h-full rounded-full transition-all duration-300', colorMap[color].fill)}
                style={{ width: `${percent}%` }}
              />
            </div>
            {/* Thumb */}
            <div
              className={cn('absolute top-1/2 -translate-y-1/2 w-4 h-4 rounded-full transition-all duration-300 pointer-events-none', colorMap[color].thumb)}
              style={{ left: `calc(${percent}% - 8px)` }}
            />
          </div>
        </div>
      );
    }

    export default Slider;
    ```

    Uses a hidden native `<input type="range">` for a11y + keyboard support, with custom visual track and thumb overlay.
  </action>
  <verify>
    - Track: `bg-white/[0.06] rounded-full h-1.5`
    - Fill: `bg-emerald` (or amber/cyan via prop)
    - Thumb: `w-4 h-4 rounded-full` with glow shadow
    - Hidden native range input for keyboard/a11y support
    - Label + value display above slider
  </verify>
</task>

<task id="6">
  <name>Create ProgressRing.tsx primitive</name>
  <files>frontend/src/components/ui/ProgressRing.tsx</files>
  <action>
    SVG circular progress gauge matching Training page Temp/Load gauges and Dashboard traffic factor circle.

    ```tsx
    import { cn } from '../../lib/utils';

    interface ProgressRingProps {
      value: number;           // 0-100
      size?: number;           // px, default 80
      strokeWidth?: number;    // px, default 6
      color?: 'emerald' | 'amber' | 'rose' | 'cyan';
      label?: string;
      children?: React.ReactNode; // center content
    }

    const colorMap = {
      emerald: { stroke: '#10B981', glow: 'rgba(16,185,129,0.2)' },
      amber: { stroke: '#F59E0B', glow: 'rgba(245,158,11,0.2)' },
      rose: { stroke: '#EF4444', glow: 'rgba(239,68,68,0.2)' },
      cyan: { stroke: '#14B8A6', glow: 'rgba(20,184,166,0.2)' },
    };

    export function ProgressRing({ value, size = 80, strokeWidth = 6, color = 'emerald', label, children }: ProgressRingProps) {
      const radius = (size - strokeWidth) / 2;
      const circumference = 2 * Math.PI * radius;
      const offset = circumference - (value / 100) * circumference;
      const c = colorMap[color];

      return (
        <div className="inline-flex flex-col items-center gap-2">
          <div className="relative" style={{ width: size, height: size }}>
            <svg width={size} height={size} className="-rotate-90">
              {/* Background track */}
              <circle cx={size/2} cy={size/2} r={radius} fill="none"
                stroke="rgba(255,255,255,0.06)" strokeWidth={strokeWidth} />
              {/* Progress arc */}
              <circle cx={size/2} cy={size/2} r={radius} fill="none"
                stroke={c.stroke} strokeWidth={strokeWidth}
                strokeDasharray={circumference} strokeDashoffset={offset}
                strokeLinecap="round"
                style={{ filter: `drop-shadow(0 0 4px ${c.glow})`, transition: 'stroke-dashoffset 500ms ease-out' }}
              />
            </svg>
            {/* Center content */}
            <div className="absolute inset-0 flex items-center justify-center">
              {children || <span className="text-sm font-bold text-white">{value}%</span>}
            </div>
          </div>
          {label && <span className="text-xs text-label">{label}</span>}
        </div>
      );
    }

    export default ProgressRing;
    ```
  </action>
  <verify>
    - SVG with circular arc, `strokeDasharray`/`strokeDashoffset`
    - Track: `rgba(255,255,255,0.06)`
    - Progress: Color from prop with drop-shadow glow
    - Center: children or percentage text
    - `transition: stroke-dashoffset 500ms ease-out` for smooth fill animation
  </verify>
</task>

<task id="7">
  <name>Rebuild remaining primitives (Badge, ProgressBar, Spinner, Skeleton, EmptyState, PageLoader)</name>
  <files>frontend/src/components/ui/Badge.tsx, frontend/src/components/ui/ProgressBar.tsx, frontend/src/components/ui/Spinner.tsx, frontend/src/components/ui/Skeleton.tsx, frontend/src/components/ui/EmptyState.tsx, frontend/src/components/ui/PageLoader.tsx</files>
  <action>
    **Badge.tsx:**
    ```
    variants = {
      emerald: 'bg-emerald-dim text-emerald',
      amber: 'bg-amber-dim text-amber',
      rose: 'bg-rose-dim text-rose',
      cyan: 'bg-cyan-dim text-cyan',
      blue: 'bg-blue-dim text-blue',
      neutral: 'bg-white/[0.06] text-label',
    }
    ```
    Base: `rounded-full text-xs font-medium px-2.5 py-0.5 inline-flex items-center gap-1.5`
    Optional dot: `w-1.5 h-1.5 rounded-full bg-current`

    **ProgressBar.tsx:**
    Track: `bg-white/[0.06] rounded-full overflow-hidden`
    Fill: Color variants matching accent palette. Default `bg-emerald`.
    Sizes: `sm` = `h-1`, `md` = `h-1.5`, `lg` = `h-2.5`
    Fill has `transition-all duration-500 ease-out`.
    ARIA: `role="progressbar"` with `aria-valuenow`

    **Spinner.tsx:**
    `border-2 border-white/[0.08] border-t-emerald rounded-full animate-spin`
    Sizes: `sm` = `w-4 h-4`, `md` = `w-6 h-6`, `lg` = `w-8 h-8`

    **Skeleton.tsx:**
    Base: `bg-white/[0.04] rounded-xl animate-pulse`
    Keep the composite skeletons (StatCardSkeleton, DashboardSkeleton, etc.) but rewrite their classes:
    - Card containers: `bg-white/[0.02] rounded-2xl border border-white/[0.05] p-5`
    - Inner rectangles: `bg-white/[0.04] rounded-lg h-4`

    **EmptyState.tsx:**
    Icon: `text-white/[0.10]` (very faint)
    Title: `text-label font-medium`
    Description: `text-muted text-sm`
    CTA: Use the new `Button` component

    **PageLoader.tsx:**
    Rebuild with skeleton card grid matching Dashboard layout:
    - 4-column stat card skeletons
    - Map-area skeleton
    - Right panel skeletons
  </action>
  <verify>
    - All 6 files updated
    - Badge uses `bg-emerald-dim text-emerald` pattern
    - ProgressBar track: `bg-white/[0.06]`, fill: `bg-emerald`, has `transition-all duration-500`
    - Spinner: `border-t-emerald animate-spin`
    - Skeleton: `bg-white/[0.04] rounded-xl animate-pulse`
    - EmptyState: `text-white/[0.10]` icon
    - No `dark:` prefixes, no `var(--`, no old color scales
  </verify>
</task>

<task id="8">
  <name>Rebuild StatCard.tsx with reference-matched design</name>
  <files>frontend/src/components/StatCard.tsx</files>
  <action>
    Rewrite to match reference Dashboard stat cards (Network Nodes, Avg Energy, Avg Time, Traffic Factor):

    ```tsx
    import { memo } from 'react';
    import { TrendingUp, TrendingDown, MoreHorizontal, type LucideIcon } from 'lucide-react';
    import { cn } from '../lib/utils';
    import { AnimatedNumber } from './ui';

    interface StatCardProps {
      title: string;
      value: number | string;
      unit?: string;
      subtitle?: string;
      trend?: { value: number; label?: string };
      icon?: LucideIcon;
      accent?: 'emerald' | 'amber' | 'cyan' | 'rose';
      children?: React.ReactNode; // For inline sparklines or progress rings
    }

    const accentMap = {
      emerald: 'text-emerald',
      amber: 'text-amber',
      cyan: 'text-cyan',
      rose: 'text-rose',
    };

    export default memo(function StatCard({ title, value, unit, subtitle, trend, icon: Icon, accent = 'emerald', children }: StatCardProps) {
      return (
        <div className="glass glass-hover p-5 group">
          {/* Header row */}
          <div className="flex items-center justify-between mb-3">
            <div className="flex items-center gap-2">
              {Icon && (
                <div className={cn('w-8 h-8 rounded-lg flex items-center justify-center', `bg-${accent}-dim`)}>
                  <Icon className={cn('w-4 h-4', accentMap[accent])} />
                </div>
              )}
              <span className="text-xs font-medium text-label uppercase tracking-wider">{title}</span>
            </div>
            <button className="opacity-0 group-hover:opacity-100 text-label hover:text-white transition-all duration-300">
              <MoreHorizontal className="w-4 h-4" />
            </button>
          </div>

          {/* Value */}
          <div className="flex items-baseline gap-2 mb-1">
            <span className="text-3xl font-bold text-white tabular-nums">
              {typeof value === 'number' ? <AnimatedNumber value={value} /> : value}
            </span>
            {unit && <span className="text-sm text-label">{unit}</span>}
          </div>

          {/* Subtitle / Trend */}
          <div className="flex items-center gap-2">
            {trend && (
              <span className={cn(
                'inline-flex items-center gap-1 text-xs font-medium rounded-full px-2 py-0.5',
                trend.value >= 0 ? 'bg-emerald-dim text-emerald' : 'bg-rose-dim text-rose',
              )}>
                {trend.value >= 0 ? <TrendingUp className="w-3 h-3" /> : <TrendingDown className="w-3 h-3" />}
                {trend.value >= 0 ? '+' : ''}{trend.value}%
                {trend.label && <span className="text-label ml-1">{trend.label}</span>}
              </span>
            )}
            {subtitle && !trend && <span className="text-xs text-muted">{subtitle}</span>}
          </div>

          {/* Optional children (sparkline, progress ring) */}
          {children && <div className="mt-3">{children}</div>}
        </div>
      );
    });
    ```

    **Key features:**
    - `.glass .glass-hover` utility classes for card surface + hover lift/glow
    - `...` menu dots appear on hover (`opacity-0 group-hover:opacity-100`)
    - Large value in `text-3xl font-bold text-white tabular-nums`
    - Trend badge: `bg-emerald-dim text-emerald` (positive) or `bg-rose-dim text-rose` (negative)
    - Children slot for inline sparklines or progress rings (Avg Energy card, Traffic Factor card)
    - Icon in a rounded colored background matching accent
  </action>
  <verify>
    - Card uses `.glass .glass-hover` classes
    - Value is `text-3xl font-bold text-white`
    - Trend badge is `bg-emerald-dim text-emerald` for positive
    - `...` dots hidden by default, visible on hover
    - Children slot works for inline content
    - No `dark:` prefixes, no old color scales
  </verify>
</task>

<task id="9">
  <name>Update ui/index.ts barrel exports</name>
  <files>frontend/src/components/ui/index.ts</files>
  <action>
    Update the barrel to reflect new/merged/deleted primitives:

    ```ts
    // Core
    export { Card, Card as GlassCard } from './Card';
    export { default as Button, Button as GlassButton } from './Button';
    export { default as Input, Input as GlassInput } from './Input';
    export { default as Badge } from './Badge';

    // Feedback
    export { default as ProgressBar } from './ProgressBar';
    export { default as ProgressRing } from './ProgressRing';
    export { default as Spinner } from './Spinner';

    // Interactive
    export { default as ToggleSwitch } from './ToggleSwitch';
    export { default as Slider } from './Slider';

    // Layout
    export { default as Skeleton, StatCardSkeleton, RouteCardSkeleton, DashboardSkeleton, AnalyticsSkeleton, TrainingSkeleton } from './Skeleton';
    export { default as EmptyState } from './EmptyState';
    export { default as PageLoader } from './PageLoader';

    // Animation
    export { default as AnimatedNumber } from './AnimatedNumber';
    export { default as PageTransition } from './PageTransition';
    ```

    Backward-compat aliases (`GlassCard`, `GlassButton`, `GlassInput`) ensure no import breaks in pages until they're rewritten in Wave 5.
  </action>
  <verify>
    - `GlassCard` is re-exported as alias for `Card`
    - `GlassButton` is re-exported as alias for `Button`
    - `GlassInput` is re-exported as alias for `Input`
    - New exports: `ToggleSwitch`, `Slider`, `ProgressRing`
    - No import errors across the project
  </verify>
</task>

<task id="10" type="checkpoint:human-verify" gate="blocking">
  <what-built>Rebuilt all UI primitives with the ultra-premium glass design: Card (single merged component with hover lift + emerald glow), Button (5 variants, press feedback, 500ms transitions), Input (emerald focus glow), ToggleSwitch (custom green/dark toggle), Slider (custom track/thumb with accent glow), ProgressRing (SVG circular gauge), Badge, ProgressBar, Spinner, Skeleton, EmptyState. StatCard rebuilt with trend badges and overflow dots.</what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev`
    2. **Dashboard stat cards:** Should show glass cards with large white numbers, icon in colored bg, trend badges, `...` on hover
    3. **General card feel:** Ultra-dark translucent glass panels. On hover, they should lift slightly and get a faint emerald glow border.
    4. **Buttons:** Primary emerald buttons should glow on hover. Ghost buttons barely visible until hovered. All transitions feel slow and fluid (500ms).
    5. **Components may not be perfectly wired** — page layouts are rebuilt in Wave 5. Focus on verifying the primitive look/feel in isolation.
    6. **Inspect any card:** `bg: rgba(255,255,255,0.02)`, `backdrop-filter: blur(40px)`, `border: 1px solid rgba(255,255,255,0.05)`
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Map Overhaul, or describe issues.</resume-signal>
</task>
