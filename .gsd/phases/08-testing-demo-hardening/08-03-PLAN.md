---
phase: 08-testing-demo-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/map/mapStyles.ts
  - frontend/src/services/api.ts
  - .gitignore
  - docker-compose.ui.yml
autonomous: true

must_haves:
  truths:
    - "App displays map with fallback background when internet is unavailable"
    - "All API calls target localhost only — no external service dependencies"
    - "Pre-trained model checkpoints are tracked in git and ship with clone"
    - "git clone plus docker compose -f docker-compose.ui.yml up gives a working demo"
  artifacts:
    - path: "frontend/src/components/map/mapStyles.ts"
      provides: "Map styles with offline fallback"
      contains: "fallback"
    - path: ".gitignore"
      provides: "Correct model file tracking patterns"
    - path: "models/sg_gan/traffic_gan_generator.keras"
      provides: "Pre-trained SG-GAN generator checkpoint"
    - path: "models/q_learning/trained_agent.pkl"
      provides: "Pre-trained Q-Learning agent"
  key_links:
    - from: "frontend/src/components/map/mapStyles.ts"
      to: "MapLibre GL"
      via: "tile source URL with error handling"
      pattern: "tiles|fallback|error"
    - from: "docker-compose.ui.yml"
      to: "models/"
      via: "volume mount"
      pattern: "models"
---

<objective>
Ensure the demo app works fully offline and ships with pre-trained model checkpoints.

Purpose: The demo must survive any environment — no internet, no pre-setup, just clone and run. Map tiles gracefully degrade, all data is local, and models are pre-trained.
Output: Offline-capable map, fixed .gitignore, verified docker-compose demo flow.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@frontend/src/components/map/mapStyles.ts
@frontend/src/services/api.ts
@.gitignore
@docker-compose.ui.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map offline fallback and external dependency audit</name>
  <files>frontend/src/components/map/mapStyles.ts, frontend/src/services/api.ts</files>
  <action>
  Make the map gracefully handle offline scenarios and audit external dependencies.

  In frontend/src/components/map/mapStyles.ts:
  1. Keep existing CartoDB tile URLs as primary source (they work great when online)
  2. Create an `OFFLINE_STYLE` variant for both dark and light modes that uses a simple solid-color background layer instead of raster tiles — the map still shows the road network overlay (local GeoJSON) even without internet
  3. Export a helper function `getMapStyle(theme: string, offline?: boolean)` that returns the appropriate style object
  4. The offline style should be a valid MapLibre style spec with a background layer only (no sources) — dark mode uses `#1a1a2e`, light mode uses `#e8e8e8`
  5. Add a comment explaining the offline fallback strategy

  In frontend/src/services/api.ts:
  1. Audit all API calls — verify every call targets `BASE_URL` (localhost) only
  2. Ensure no hardcoded external URLs exist (no direct calls to external weather/traffic APIs)
  3. If any external calls exist, wrap them with try/catch and return fallback data
  4. This should be a verification task — the current code likely already targets only localhost

  Note: The network graph overlay (nodes, edges, routes) is all local GeoJSON data rendered by MapLibre — it works without internet. Only the basemap tiles need internet. The offline fallback replaces the basemap with a solid color so the overlay is still visible and readable.
  </action>
  <verify>Verify that `getMapStyle('dark', true)` returns a valid MapLibre style without remote tile URLs. Grep `frontend/src/services/` for any non-localhost URLs.</verify>
  <done>Map gracefully degrades offline — shows colored background with network overlay. No external API dependencies in frontend services.</done>
</task>

<task type="auto">
  <name>Task 2: Model checkpoints and demo-ready packaging</name>
  <files>.gitignore, docker-compose.ui.yml</files>
  <action>
  Ensure pre-trained model checkpoints ship with the repo and the demo works out of the box.

  In .gitignore:
  1. Check current patterns — `models/*.h5` and `models/*.keras` only match root of models/, NOT subdirectories
  2. Verify with `git status models/` whether subdirectory model files are tracked or untracked
  3. If model files in subdirs (sg_gan/, q_learning/) are NOT tracked, run `git add` to track them
  4. Remove or update exclusion patterns to ensure pre-trained checkpoints ARE tracked
  5. Add `models/**/*.h5` to exclude any legacy .h5 files that might be regenerated (we migrated to .keras)
  6. Add a comment: `# Pre-trained checkpoints (.keras, .pkl) are intentionally tracked for instant demo`
  7. Run `git add models/sg_gan/*.keras models/q_learning/trained_agent.pkl` to ensure they're tracked

  In docker-compose.ui.yml:
  1. Verify the backend service mounts `./models:/app/models` (it should already)
  2. Add a simple healthcheck to the backend service:
     `healthcheck:`
       `test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]`
       `interval: 30s`
       `timeout: 10s`
       `retries: 3`
  3. Add `restart: unless-stopped` to both services for resilience

  Verify the demo flow conceptually:
  - `git clone` brings models/ with pre-trained checkpoints
  - `docker compose -f docker-compose.ui.yml up` starts backend (loads checkpoints) + frontend
  - User opens localhost:5173 — working demo with pre-trained models
  </action>
  <verify>Run `git ls-files models/` to confirm checkpoint files are tracked. Check docker-compose.ui.yml has healthcheck.</verify>
  <done>Model checkpoints tracked in git. Docker-compose has healthcheck and restart policy. Clone-to-demo flow verified.</done>
</task>

</tasks>

<verification>
- `git ls-files models/` shows .keras and .pkl files
- mapStyles.ts exports offline fallback style function
- No external API URLs in frontend services
- docker-compose.ui.yml has healthcheck on backend
</verification>

<success_criteria>
- Map displays road network overlay even without internet (solid background fallback)
- All frontend API calls target only localhost
- Pre-trained model checkpoints (sg_gan/*.keras, q_learning/trained_agent.pkl) tracked in git
- docker-compose.ui.yml has healthcheck and restart policy
- git clone + docker compose up = working demo
</success_criteria>

<output>
After completion, create `.gsd/phases/08-testing-demo-hardening/08-03-SUMMARY.md`
</output>
