---
phase: 08-testing-demo-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - conftest.py
  - tests/test_api.py
  - frontend/src/test-setup.ts
  - frontend/src/__tests__/PresentationMode.test.tsx
  - frontend/src/__tests__/AnimatedComponents.test.tsx
autonomous: true

must_haves:
  truths:
    - "Backend tests run without initializing TensorFlow or real EVRoutingSystem"
    - "Frontend component tests handle Framer Motion animated components without flakiness"
    - "MapLibre GL is mocked in vitest so MapView components can be tested in jsdom"
    - "Presentation mode toggle test verifies Ctrl+Shift+P adds .presentation CSS class"
  artifacts:
    - path: "conftest.py"
      provides: "DI mock fixtures for backend tests"
      contains: "app.dependency_overrides"
    - path: "tests/test_api.py"
      provides: "Backend integration tests using DI mocks"
      contains: "dependency_overrides"
    - path: "frontend/src/test-setup.ts"
      provides: "Framer Motion and MapLibre GL mocks"
      contains: "framer-motion"
    - path: "frontend/src/__tests__/PresentationMode.test.tsx"
      provides: "Presentation mode toggle test"
      contains: "presentationMode"
  key_links:
    - from: "conftest.py"
      to: "backend/app/state.py"
      via: "DI override for get_state"
      pattern: "dependency_overrides.*get_state"
    - from: "frontend/src/test-setup.ts"
      to: "framer-motion"
      via: "vi.mock"
      pattern: "vi\\.mock.*framer-motion"
---

<objective>
Establish proper test isolation for both backend and frontend test suites.

Purpose: Backend tests currently initialize real TensorFlow models (slow, fragile). Frontend tests lack Framer Motion mocks (animation timing issues). This plan fixes both, making the test suite fast and reliable.
Output: DI-based backend test fixtures, Framer Motion/MapLibre mocks, presentation mode and animation tests.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@conftest.py
@tests/test_api.py
@backend/app/state.py
@backend/app/dependencies.py
@frontend/src/test-setup.ts
@frontend/src/__tests__/Header.test.tsx
@frontend/src/hooks/usePresentationMode.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend DI mock overrides and test refactor</name>
  <files>conftest.py, tests/test_api.py</files>
  <action>
  Refactor backend tests to use FastAPI dependency injection overrides instead of real state.

  In conftest.py:
  1. Add a `mock_app_state` fixture that creates a lightweight AppState with `system=None` and empty caches
  2. Add a `client` fixture that uses `app.dependency_overrides[get_state] = lambda: mock_state` to inject the mock
  3. Use `yield` pattern so overrides are cleaned up after each test
  4. Import `app` from `backend.app.main` and `get_state` from `backend.app.state`

  In tests/test_api.py:
  1. Remove the local `client` fixture — use the conftest one
  2. Tests that need a real system (like training) should use a separate `real_client` fixture or skip
  3. Health endpoint tests should work with mock state (return degraded/mock status)
  4. Architecture tests (checking router presence, middleware) work with mock state
  5. Security header tests work with mock state
  6. Keep `reset_state()` call in teardown for any test that does use real state

  Key: The goal is that `pytest tests/test_api.py` does NOT import TensorFlow unless a test specifically needs it. The `mock_app_state` fixture should use `MagicMock` for `system` attribute so attribute access doesn't fail.
  </action>
  <verify>Run `python -m pytest tests/test_api.py -v --timeout=60` — tests pass with DI mock overrides</verify>
  <done>Backend tests isolated via DI dependency_overrides — mock AppState injected. Tests that don't need real ML models no longer trigger TF initialization.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend test mocks and presentation mode tests</name>
  <files>frontend/src/test-setup.ts, frontend/src/__tests__/PresentationMode.test.tsx, frontend/src/__tests__/AnimatedComponents.test.tsx</files>
  <action>
  Add proper test mocks and write missing test coverage.

  In frontend/src/test-setup.ts:
  1. Add `vi.mock('framer-motion', ...)` that replaces `motion` components with plain HTML elements (e.g., `motion.div` becomes `div`), `AnimatePresence` becomes passthrough children, `useAnimation` becomes noop
  2. Add `vi.mock('maplibre-gl', ...)` that exports a mock Map class with `on()`, `remove()`, `addSource()`, `addLayer()` stubs
  3. Add `vi.mock('react-map-gl', ...)` that exports mock Map and Marker components
  4. Keep existing `@testing-library/jest-dom` setup

  Create frontend/src/__tests__/PresentationMode.test.tsx:
  1. Test that store's `togglePresentationMode()` flips the boolean
  2. Test that the `usePresentationMode` hook adds `.presentation` class to `document.documentElement` when enabled
  3. Test that keyboard shortcut Ctrl+Shift+P triggers the toggle (simulate keydown event)
  4. Use `@testing-library/react` `renderHook` for the hook test

  Create frontend/src/__tests__/AnimatedComponents.test.tsx:
  1. Test that AnimatedNumber renders a number value
  2. Test that PageTransition renders children
  3. These verify the Framer Motion mock works correctly — components render without errors

  Use existing patterns from Header.test.tsx for structure.
  </action>
  <verify>Run `cd frontend && npx vitest run --reporter=verbose` — all tests pass including new ones</verify>
  <done>Framer Motion and MapLibre mocked in test-setup.ts. PresentationMode and AnimatedComponents tests pass. Existing tests still pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_api.py -v` passes with DI mocks
- `cd frontend && npx vitest run` passes all unit tests
- No TensorFlow import in mock-based backend test runs
- Framer Motion mock prevents animation timing issues
</verification>

<success_criteria>
- Backend tests isolated via DI dependency_overrides — mock AppState injected
- Frontend test-setup.ts has Framer Motion + MapLibre mocks
- PresentationMode tests verify toggle, CSS class, and keyboard shortcut
- AnimatedComponents tests verify mock works for animated components
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.gsd/phases/08-testing-demo-hardening/08-01-SUMMARY.md`
</output>
