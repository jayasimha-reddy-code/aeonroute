---
phase: 07-ml-pipeline-upgrade
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - backend/app/routers/analytics.py
  - frontend/src/pages/Analytics.tsx
  - frontend/src/services/api.ts
autonomous: true

must_haves:
  truths:
    - "Analytics page shows real GAN evaluation metrics (hourly correlation, peak ratios, distribution stats)"
    - "Analytics page shows real agent performance metrics (success rate, avg reward, Q-table size, convergence status)"
    - "Analytics page shows real route generation metrics (feasibility rate, energy improvement, avg candidates)"
    - "Training loss/reward history charts display actual data from last training run"
    - "No hardcoded synthetic chart data remains in Analytics.tsx"
  artifacts:
    - path: "backend/app/routers/analytics.py"
      provides: "Comprehensive evaluation metrics endpoints"
      contains: "gan-evaluation"
    - path: "frontend/src/pages/Analytics.tsx"
      provides: "Analytics page consuming real API data"
      contains: "gan-evaluation"
  key_links:
    - from: "backend/app/routers/analytics.py"
      to: "src/evaluate.py"
      via: "SystemEvaluator methods called from endpoints"
      pattern: "evaluate_gan|evaluate_agent|evaluate_route"
    - from: "frontend/src/pages/Analytics.tsx"
      to: "backend/app/routers/analytics.py"
      via: "API fetch calls to analytics endpoints"
      pattern: "api.*analytics|gan-evaluation|agent-performance"
---

<objective>
Wire comprehensive ML evaluation metrics to the Analytics page with real data.

Purpose: Replace hardcoded synthetic chart data in Analytics.tsx with real metrics from the evaluation pipeline. Add backend endpoints that expose GAN quality, agent performance, and route generation metrics from SystemEvaluator. The Analytics page becomes the proof that the ML pipeline produces quality results.
Output: Real-data-driven Analytics page with GAN metrics, agent stats, training history, and route evaluation.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/07-ml-pipeline-upgrade/07-01-SUMMARY.md
@.gsd/phases/07-ml-pipeline-upgrade/07-02-SUMMARY.md
@backend/app/routers/analytics.py
@frontend/src/pages/Analytics.tsx
@src/evaluate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add comprehensive evaluation endpoints to analytics router</name>
  <files>backend/app/routers/analytics.py</files>
  <action>
  Add four new endpoints to the analytics router (backend/app/routers/analytics.py):

  **1. GET /api/analytics/gan-evaluation**
  Uses SystemEvaluator.evaluate_gan() to return GAN quality metrics:
  `python
  @router.get("/analytics/gan-evaluation", summary="GAN training quality metrics")
  async def get_gan_evaluation(request: Request, state: AppState = Depends(get_state)):
      if state.system is None or state.system.gan is None:
          return fail("GAN not trained yet", 503)
      from src.evaluate import SystemEvaluator
      evaluator = SystemEvaluator()
      evaluator.road_graph = state.system.road_graph
      evaluator.gan = state.system.gan
      results = evaluator.evaluate_gan(num_samples=50)
      return ok(results)
  `

  **2. GET /api/analytics/agent-performance**
  Uses SystemEvaluator.evaluate_agent() to return agent stats:
  `python
  @router.get("/analytics/agent-performance", summary="Q-Learning agent performance")
  async def get_agent_performance(request: Request, state: AppState = Depends(get_state)):
      if state.system is None or state.system.agent is None:
          return fail("Agent not trained yet", 503)
      from src.evaluate import SystemEvaluator
      evaluator = SystemEvaluator()
      evaluator.road_graph = state.system.road_graph
      evaluator.agent = state.system.agent
      results = evaluator.evaluate_agent(num_episodes=20)
      return ok(results)
  `

  **3. GET /api/analytics/training-history**
  Returns stored training loss/reward history from the last training run:
  `python
  @router.get("/analytics/training-history", summary="Training loss and reward history")
  async def get_training_history(request: Request, state: AppState = Depends(get_state)):
      return ok({
          "loss_history": state.training_status.get("loss_history", []),
          "reward_history": state.training_status.get("reward_history", []),
          "metrics": state.training_status.get("metrics", {}),
      })
  `

  **4. GET /api/analytics/route-evaluation**
  Uses SystemEvaluator.evaluate_route_generation() to return route quality metrics:
  ```python
  @router.get("/analytics/route-evaluation", summary="Route generation quality metrics")
  async def get_route_evaluation(request: Request, state: AppState = Depends(get_state)):
      if state.system is None or state.system.route_generator is None:
          return fail("Route generator not ready", 503)
      from src.evaluate import SystemEvaluator
      evaluator = SystemEvaluator()
      evaluator.road_graph = state.system.road_graph
      evaluator.route_generator = state.system.route_generator
      results = evaluator.evaluate_route_generation(num_tests=10)
      return ok(results)
  ```

  Import SystemEvaluator at the top of the file. Run evaluations with reduced sample counts (50 GAN, 20 agent, 10 routes) to keep response times under 5 seconds.

  IMPORTANT: The evaluate_gan and evaluate_agent calls involve CPU-bound computation. Wrap them in syncio.get_event_loop().run_in_executor(None, ...) to avoid blocking the event loop.
  </action>
  <verify>
  python -c "from backend.app.routers.analytics import router; print([r.path for r in router.routes])" shows the new endpoints
  grep -c "gan-evaluation\|agent-performance\|training-history\|route-evaluation" backend/app/routers/analytics.py returns 4+
  </verify>
  <done>Four new analytics endpoints return real GAN metrics, agent performance, route evaluation quality, and training history. Evaluations run in executor to avoid blocking. Response times under 5 seconds with reduced sample counts.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded Analytics data with real API calls</name>
  <files>frontend/src/pages/Analytics.tsx, frontend/src/services/api.ts</files>
  <action>
  **API service (frontend/src/services/api.ts):**
  Add four new API functions (or extend existing patterns):
  `	ypescript
  getGanEvaluation: () => api.get('/api/analytics/gan-evaluation'),
  getAgentPerformance: () => api.get('/api/analytics/agent-performance'),
  getRouteEvaluation: () => api.get('/api/analytics/route-evaluation'),
  getTrainingHistory: () => api.get('/api/analytics/training-history'),
  `

  **Analytics page (frontend/src/pages/Analytics.tsx):**
  The page currently has hardcoded synthetic data for charts (around lines 69-99). Replace ALL hardcoded data with real API calls:

  1. Add four useApi hooks (or useEffect + useState) to fetch from the new endpoints on mount:
     - ganMetrics from /api/analytics/gan-evaluation
     - agentMetrics from /api/analytics/agent-performance
     - routeMetrics from /api/analytics/route-evaluation
     - trainingHistory from /api/analytics/training-history

  2. Replace the hardcoded "Energy Consumption Timeline" chart data with real route metrics trend (from training-history or route-metrics)

  3. Replace the hardcoded "GAN Training Convergence" chart with real loss_history data:
     - X axis: epoch number
     - Y axis: g_loss and d_loss_real lines (from training-history endpoint)

  4. Replace the hardcoded "Route Quality Distribution" donut chart with real agent performance data:
     - Show success_rate as a percentage donut
     - Show avg_reward, num_states as stat cards

  5. Replace the hardcoded "Distance Distribution" chart with real GAN evaluation data:
     - Show hourly_correlation, morning_peak_ratio, evening_peak_ratio as bar chart or metrics

  6. Add a new section showing comprehensive metrics in a grid:
     - GAN Quality: hourly_correlation, generated_mean, generated_std, peak ratios
     - Agent: success_rate, avg_reward, avg_steps, num_states, convergence status
     - Routes: avg_candidates, avg_feasibility_rate, energy_improvement

  Keep the existing glassmorphism card styling and Framer Motion animations from Phase 03. Use the existing Recharts components. Show skeleton loading states while data loads.

  Handle the case where models aren't trained yet — show "Train models first" message with link to Training page, not empty charts.
  </action>
  <verify>
  
px tsc --noEmit from frontend/ directory — zero TypeScript errors
  Visual check: Analytics page loads with real data (or graceful "not trained" message)
  No hardcoded arrays of chart data remain: grep -n "data:" frontend/src/pages/Analytics.tsx | head -20 (should show API data, not synthetic arrays)
  </verify>
  <done>Analytics page fetches real data from 3 new API endpoints. GAN training convergence chart shows real loss curves. Agent metrics show success rate and reward. Route metrics show feasibility and energy improvement. No hardcoded synthetic data remains. Graceful loading and error states.</done>
</task>

</tasks>

<verification>
1. Backend endpoints respond: curl http://localhost:8000/api/analytics/training-history returns JSON
2. Frontend compiles: cd frontend && npx tsc --noEmit
3. No hardcoded chart data: grep -c "synthetic\|hardcoded\|dummy" frontend/src/pages/Analytics.tsx returns 0
4. Analytics page shows real metrics when models are trained
5. Analytics page shows "Train first" when models aren't loaded
</verification>

<success_criteria>
- GET /api/analytics/gan-evaluation returns real GAN quality metrics
- GET /api/analytics/agent-performance returns real agent stats
- GET /api/analytics/route-evaluation returns route generation quality metrics
- GET /api/analytics/training-history returns stored loss/reward history
- Analytics.tsx uses only real API data (zero hardcoded chart arrays)
- Graceful degradation when models aren't trained
- Existing glassmorphism styling and animations preserved
</success_criteria>

<output>
After completion, create .gsd/phases/07-ml-pipeline-upgrade/07-04-SUMMARY.md
</output>
