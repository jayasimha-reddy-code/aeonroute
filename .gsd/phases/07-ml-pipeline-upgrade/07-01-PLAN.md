---
phase: 07-ml-pipeline-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/traffic_generator.py
  - models/sg_gan/traffic_gan_generator.h5
  - models/sg_gan/traffic_gan_discriminator.h5
autonomous: true

must_haves:
  truths:
    - "SG-GAN discriminator uses SpectralNormalization on all Dense layers"
    - "GAN training applies gradient clipping (clipnorm=1.0) on both G and D optimizers"
    - "SG-GAN training completes 100 epochs without NaN loss values"
    - "No .h5 fallback code remains in traffic_generator.py load methods"
    - "No .h5 files remain in models/sg_gan/ directory"
  artifacts:
    - path: "src/traffic_generator.py"
      provides: "Stable SG-GAN with spectral normalization and gradient clipping"
      contains: "SpectralNormalization"
  key_links:
    - from: "src/traffic_generator.py"
      to: "tf.keras.layers.SpectralNormalization"
      via: "Wrapping discriminator Dense layers"
      pattern: "SpectralNormalization.*Dense"
---

<objective>
Stabilize SG-GAN training and clean up legacy model format.

Purpose: Prevent NaN loss and mode collapse during demo by adding spectral normalization to the discriminator and gradient clipping to both optimizers. Also remove all legacy .h5 save/load code and delete stale .h5 files from models/sg_gan/.
Output: Modified traffic_generator.py with stable training, .keras-only model persistence.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@src/traffic_generator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spectral normalization and gradient clipping to SG-GAN</name>
  <files>src/traffic_generator.py</files>
  <action>
  Modify the discriminator uild_discriminator() method (around line 302-382) to wrap every Dense layer with 	f.keras.layers.SpectralNormalization. The current architecture uses 3 Dense layers (512, 256, 128) with LeakyReLU and LayerNormalization. Replace the plain Dense calls:

  `python
  # Before
  x = tf.keras.layers.Dense(512)(x)
  # After
  x = tf.keras.layers.SpectralNormalization(tf.keras.layers.Dense(512))(x)
  `

  Keep LayerNormalization and Dropout(0.3) as-is — spectral norm goes ON the Dense layer, not replacing LayerNorm.

  Modify the optimizers (around line 437-438) to add gradient clipping:

  `python
  # Before
  self.d_optimizer = tf.keras.optimizers.Adam(learning_rate=0.0002, beta_1=0.5)
  self.g_optimizer = tf.keras.optimizers.Adam(learning_rate=0.0002, beta_1=0.5)
  # After
  self.d_optimizer = tf.keras.optimizers.Adam(learning_rate=0.0002, beta_1=0.5, clipnorm=1.0)
  self.g_optimizer = tf.keras.optimizers.Adam(learning_rate=0.0002, beta_1=0.5, clipnorm=1.0)
  `

  Also add a NaN guard in the training step (around line 461-510). After computing losses, check for NaN and skip the gradient application if detected:

  `python
  if tf.math.is_nan(g_loss) or tf.math.is_nan(d_loss):
      tf.print("WARNING: NaN detected, skipping gradient update")
      return {"g_loss": 0.0, "d_loss_real": 0.0, "d_loss_fake": 0.0}
  `

  Do NOT change the loss function (keep BinaryCrossentropy) or the training loop structure. Do NOT add Wasserstein loss — that would be a larger change.
  </action>
  <verify>
  Run python -c "from src.traffic_generator import SGGANTrafficGenerator; g = SGGANTrafficGenerator(); print('SpectralNormalization' in str(g.discriminator.get_config()))" — should print True or at least not error.
  Verify no import errors: python -c "from src.traffic_generator import SGGANTrafficGenerator; print('OK')"
  </verify>
  <done>Discriminator Dense layers wrapped in SpectralNormalization. Both optimizers have clipnorm=1.0. NaN guard in training step. Code imports and instantiates without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Remove .h5 fallback code and delete legacy .h5 files</name>
  <files>src/traffic_generator.py, models/sg_gan/traffic_gan_generator.h5, models/sg_gan/traffic_gan_discriminator.h5</files>
  <action>
  In traffic_generator.py, find the load() method (around line 700-720) which has a fallback that tries .h5 if .keras loading fails. Remove the .h5 fallback branch entirely — load should only try .keras format. The current code looks like:

  `python
  try:
      self.generator = tf.keras.models.load_model(f"{path}_generator.keras")
  except:
      self.generator = tf.keras.models.load_model(f"{path}_generator.h5")
  `

  Change to only load .keras:

  `python
  self.generator = tf.keras.models.load_model(f"{path}_generator.keras")
  self.discriminator = tf.keras.models.load_model(f"{path}_discriminator.keras")
  `

  Keep proper error handling (try/except with meaningful message), just remove the .h5 fallback path.

  Also verify the save() method (around line 680) already saves as .keras — if it does, no change needed there.

  Delete the legacy .h5 files from disk:
  - models/sg_gan/traffic_gan_generator.h5
  - models/sg_gan/traffic_gan_discriminator.h5

  Use os.remove() or filesystem deletion. These files are 26MB+ dead weight.
  </action>
  <verify>
  ls models/sg_gan/ should show only .keras files, no .h5 files.
  python -c "from src.traffic_generator import SGGANTrafficGenerator; print('OK')" still works.
  grep -n "\.h5" src/traffic_generator.py should return zero matches (or only in comments).
  </verify>
  <done>No .h5 fallback code in traffic_generator.py load method. No .h5 files in models/sg_gan/ directory. Only .keras format used for save and load.</done>
</task>

</tasks>

<verification>
1. python -c "from src.traffic_generator import SGGANTrafficGenerator; g = SGGANTrafficGenerator(); print('Build OK')" succeeds
2. ls models/sg_gan/ shows only .keras files
3. grep -rn "\.h5" src/traffic_generator.py returns empty or comments-only
4. SpectralNormalization is present in discriminator architecture
</verification>

<success_criteria>
- SG-GAN discriminator has SpectralNormalization on all Dense layers
- Both G and D optimizers use clipnorm=1.0
- NaN guard prevents gradient updates with NaN losses
- No .h5 fallback code remains
- No .h5 files in models/sg_gan/
</success_criteria>

<output>
After completion, create .gsd/phases/07-ml-pipeline-upgrade/07-01-SUMMARY.md
</output>
