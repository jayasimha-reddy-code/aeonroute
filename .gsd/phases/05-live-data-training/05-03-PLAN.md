---
phase: 05-live-data-training
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/src/components/dashboard/TrafficSlider.tsx
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/services/api.ts
autonomous: true

must_haves:
  truths:
    - "Time-of-day traffic slider on dashboard updates heatmap showing SG-GAN temporal patterns"
    - "Dashboard auto-refreshes system stats every 30 seconds via polling"
    - "Slider shows hours 0-23 with labeled tick marks and heatmap grid responds to slider changes"
  artifacts:
    - path: "frontend/src/components/dashboard/TrafficSlider.tsx"
      provides: "Time-of-day slider with traffic heatmap grid visualization"
      exports: ["TrafficSlider"]
    - path: "frontend/src/pages/Dashboard.tsx"
      provides: "Dashboard with TrafficSlider card and 30s auto-refresh"
      contains: "TrafficSlider"
    - path: "frontend/src/services/api.ts"
      provides: "getTemporalTraffic API method"
      contains: "traffic-patterns/temporal"
  key_links:
    - from: "frontend/src/components/dashboard/TrafficSlider.tsx"
      to: "frontend/src/services/api.ts"
      via: "Calls getTemporalTraffic on mount"
      pattern: "getTemporalTraffic"
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "frontend/src/components/dashboard/TrafficSlider.tsx"
      via: "Renders TrafficSlider in dashboard grid"
      pattern: "TrafficSlider"
---

<objective>
Create the time-of-day traffic slider and dashboard auto-refresh -- the visual proof that
the SG-GAN has learned temporal traffic patterns.

Purpose: The traffic slider lets evaluators drag through 24 hours and see how the AI models
time-of-day traffic variation. Combined with dashboard auto-refresh, this keeps the
dashboard feeling alive and responsive even when training is not active.

Output: TrafficSlider component on Dashboard with 24h heatmap, plus 30s auto-refresh for
system stats.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/05-live-data-training/05-RESEARCH.md
@.gsd/phases/05-live-data-training/05-01-SUMMARY.md

@frontend/src/pages/Dashboard.tsx
@frontend/src/services/api.ts
@backend/app/routers/analytics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrafficSlider component + API method</name>
  <files>
    frontend/src/components/dashboard/TrafficSlider.tsx
    frontend/src/services/api.ts
  </files>
  <action>
    **1. Add getTemporalTraffic to api.ts:**
    - Add interface TemporalTrafficData: { grid_size: number, time_steps: number, traffic: number[][] }
    - Add method to APIClient:
      async getTemporalTraffic(): Promise<TemporalTrafficData>
      Calls GET /api/traffic-patterns/temporal, returns response.data.data
    - Export the TemporalTrafficData type

    **2. Create `frontend/src/components/dashboard/` directory** if it does not exist.

    **3. Create TrafficSlider.tsx:**
    - On mount, call api.getTemporalTraffic() to fetch full 24h traffic grid
    - Store result in local state: trafficData (number[][] where shape is num_roads x 24)
    - Range slider input: min=0, max=23, step=1, default=12
    - Display current hour label: "12:00 PM" (format hour as 12h with AM/PM)
    - Add labeled tick marks at 0, 6, 12, 18, 23 (midnight, 6am, noon, 6pm, 11pm)
    - Render traffic as a color-coded grid:
      - Grid dimensions: grid_size x grid_size (e.g. 10x10)
      - Each cell color = traffic intensity at selected hour for that road segment
      - Color scale: green (low traffic, 0.0-0.3) -> yellow (medium, 0.3-0.6) -> red (high, 0.6-1.0)
      - Use inline backgrounds or Tailwind classes: bg-green-500, bg-yellow-500, bg-red-500 with opacity
      - Actually, for smooth gradient, use inline style with interpolated hsl or rgb values:
        e.g. low=hsl(120,70%,50%), med=hsl(60,70%,50%), high=hsl(0,70%,50%)
    - Show loading skeleton while fetching
    - Show "Train a model first to see traffic patterns" message if API returns error (no model)
    - Style: glass card aesthetic matching project design system
    - Wrap grid in a container with max-width to keep cells visible
    - Add legend strip showing color scale: Low -> Medium -> High
    - Style the slider with accent/primary colors from the design system

    **4. Handle edge cases:**
    - If trafficData is null/loading, show skeleton
    - If API returns 503 (no system), show friendly message
    - If grid_size is 0 or traffic is empty, show "No data available"
  </action>
  <verify>
    Run: cd frontend && npx tsc --noEmit -- zero TypeScript errors.
    Grep for getTemporalTraffic in services/api.ts.
    Verify TrafficSlider.tsx exists with slider input and grid rendering.
  </verify>
  <done>
    API client has getTemporalTraffic method calling /api/traffic-patterns/temporal.
    TrafficSlider fetches 24h traffic data on mount, renders color-coded grid at selected hour.
    Slider ranges 0-23 hours with labeled ticks and formatted hour display.
    Color scale: green (low) -> yellow (medium) -> red (high) with legend.
    Handles loading, error, and no-data states gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard integration + auto-refresh</name>
  <files>
    frontend/src/pages/Dashboard.tsx
  </files>
  <action>
    **1. Add TrafficSlider to Dashboard.tsx:**
    - Import TrafficSlider from '../components/dashboard/TrafficSlider'
    - Add a new Card in the dashboard grid (below the existing cards, spanning full width
      or 2 columns on lg screens) containing the TrafficSlider
    - Card header: clock icon + "Time-of-Day Traffic Patterns" title
    - Card subtitle: "SG-GAN learned temporal traffic variation"
    - Only show this card when system is initialized (check existing system state)

    **2. Add 30-second auto-refresh to Dashboard:**
    - Create a useAutoRefresh hook (or inline useEffect) that calls the dashboard data
      refresh function every 30 seconds
    - The refresh should re-fetch system stats (api.getSystemStats or equivalent)
    - Store refresh state so the UI can show a subtle refresh indicator (optional small
      spinning icon or timestamp "Last updated: 30s ago")
    - Clear interval on unmount
    - Do NOT auto-refresh when the browser tab is hidden (use document.hidden check)
    - This satisfies LIVE-06 (Dashboard auto-refresh with polling)

    **3. Wrap TrafficSlider card appearance in Framer Motion:**
    - Use motion.div with initial opacity 0, animate opacity 1
    - Consistent with existing dashboard animation patterns
  </action>
  <verify>
    Run: cd frontend && npx tsc --noEmit -- zero TypeScript errors.
    Run: cd frontend && npx vitest run -- existing tests pass.
    Grep: Dashboard.tsx imports TrafficSlider.
    Grep: Dashboard.tsx contains setInterval or useEffect with 30000 ms interval.
  </verify>
  <done>
    Dashboard renders TrafficSlider in a new card showing SG-GAN temporal patterns.
    Dashboard stats auto-refresh every 30 seconds (stops when tab is hidden).
    TrafficSlider card appears with smooth Framer Motion animation.
    All TypeScript compiles, existing tests pass.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: cd frontend && npx tsc --noEmit -- 0 errors
2. Existing tests pass: cd frontend && npx vitest run
3. TrafficSlider renders 10x10 grid with colors changing as slider moves
4. Dashboard refreshes system stats every 30s
5. Traffic slider shows "Train a model first" when no GAN is trained
</verification>

<success_criteria>
- Dragging the time-of-day slider updates the traffic heatmap grid in real-time
- Dashboard auto-refreshes every 30 seconds
- Color scale clearly shows traffic intensity variation across hours
- Works gracefully when no model is trained (friendly message, no crash)
</success_criteria>

<output>
After completion, create .gsd/phases/05-live-data-training/05-03-SUMMARY.md
</output>